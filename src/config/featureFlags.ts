/**
 * Feature flags for hiding incomplete features before demo.
 * Flip to true to re-enable.
 */
export const FEATURE_FLAGS = {
  ENABLE_VIEW_AS: false,
  ENABLE_VOICE_INPUT: false,
  ENABLE_BETA_FEEDBACK: true,
} as const
