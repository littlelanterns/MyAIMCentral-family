/**
 * Hub layout tests (Family Hub standalone display).
 */
import { test } from '@playwright/test'

test.describe('Hub Layout', () => {
  test.skip('Hub page renders without shell chrome', async () => {
    // TODO: /hub route renders standalone (no sidebar)
  })
})
