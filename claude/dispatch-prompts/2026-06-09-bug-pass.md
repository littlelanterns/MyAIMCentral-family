# Dispatch — Bug Pass: Capture & Route + Small UX (2026-06-09)

> Paste everything below the line into a fresh Claude Code session.

---

You are Worker BUG-PASS-0609 on MyAIM Central. This is a feature-anchored bug-fix session — a full build per the orchestrator rules: Checkpoint 2-style verification per item, Checkpoint 5 audit table at the end, close-out file updates. CLAUDE.md and `.claude/rules/` auto-load; honor every convention, especially:

- **#257** — no new client-side `todayLocalIso()` writes to DATE columns; "today" filters use server-derived dates; use helpers in `src/utils/dates.ts`
- **#121** — `./node_modules/.bin/tsc -b` clean before declaring anything done
- **#272** — any Realtime channel work: per-instance `useId()` channel names, `.on()` before `.subscribe()`, `removeChannel` cleanup
- Theme tokens only (no hardcoded colors), Lucide icons (no emoji), `<ModalV2>` for modals, density classes
- Visual Verification Standard: hard reload + eyes-on at desktop AND 375px mobile before marking any UI item Wired
- **Do NOT change `beta_glitch_reports.status`** — append to `admin_notes` only; the founder flips statuses after her own verification
- After any migration: `npm run schema:dump` and stage `claude/live_schema.md` (pre-commit hook enforces)

## Scope — 8 fixes + 1 verify, in this order

### 1. NEW-AAA (worksheet row 207 · bug `8779cc41`) — Notepad mobile routing actions hidden — HIGH, Beta=Y
On mobile, the Smart Notepad drawer's **"Send to…" and "Review & Route"** actions are clipped/hidden, blocking the core capture→route flow on phones. Fix the NotepadDrawer mobile layout/overflow so both actions are reachable at 375px. Remember: LiLa drawer sits above bottom nav (`bottom-14 md:bottom-0`) — Notepad has similar constraints. Eyes-on 375px evidence required.

### 2. Row 31 SCOPE-4.F1 (LOCKED Fix Code, founder green-lit 2026-04-20) — MindSweep silent failure class
(a) Migration (next number ≥ `00000000100250_`): add `embedding halfvec(1536)` + HNSW index + `util.queue_embedding_job()` trigger to `archive_context_items`; backfill existing rows **via the async pgmq queue** (never synchronous — Convention #5).
(b) Replace the silent catch in `supabase/functions/mindsweep-sort/index.ts:129-156` with structured logging; split the `ai_parse` feature_key per the row's note.
Redeploy `mindsweep-sort`.

### 3. NEW-QQQ (row 223 · bug `16e94c3b`) — /sweep "sweep all" error after saved image — MEDIUM
Repro: save an image capture "for later" on /sweep, then run "Sweep all" → "something went wrong." Use the new structured logging from item 2 to diagnose. Likely suspects: image-type `mindsweep_holding` rows entering a text-only classify path. Fix the root cause; add a regression test if feasible.

### 4. NEW-DDD (row 210 · bug `30f77dd9`) — phantom sidebar Messages badge — MEDIUM, Beta=Y
Founder-clarified with screenshot: the **sidebar Family > Messages badge shows "1"** while `/messages` shows nothing unread or openable. Reconcile the sidebar unread-count query against what the Messages page actually surfaces. Suspects: stale `message_read_status`, counting the member's own messages, or unread in a space/thread the list view doesn't render (e.g. content_corner). The badge and the page MUST derive from the same definition of "unread and visible."

### 5. NEW-CCC (row 209 · bug `1186a977`) — Calendar event creation date UX — MEDIUM, Beta=Y
Two parts: (1) EventCreationModal "One time" pre-fills today's date in a way that misleads (event saved correctly; the *display* confused) — make the chosen date unambiguous; (2) add an edit affordance for `leave_by_time` on existing events (mom could set it at creation but not edit it later).

### 6. NEW-FFF (row 212 · bug `4e9b96c3`) — task due-date discoverability — MEDIUM, Beta=Y
Due-date editing only surfaces when schedule mode is `one_time`; from the task card edit flow it's hard to find. Surface a simple date affordance in task quick-edit OR default the schedule section open in edit mode. UX fix, not a data change.

### 7. NEW-HHH (row 214 · bug `e8a8d506`) — phantom "Save to Studio" hint — LOW
Routine edit mode in TaskCreationModal shows hint text pointing at a "Save to Studio" button that doesn't exist in that mode. Correct the copy or add the affordance — check how `handleSaveToStudio` is actually reachable and make hint match reality.

### 8. NEW-GGG (row 213 · bug `cefc6ca8`) — collapsible Settings sections — LOW
Settings page sections collapse, **default closed**. Reuse the Sidebar Collapsible Sections pattern from `claude/conventions.md` (max-height transition, 200ms). Session-only state is fine.

### 9. NEW-EEE (row 211 · bug `96f2d58e`) — inline edit of past reflections — MEDIUM, Beta=Y
Add inline editing of `reflection_responses.response_text` in the past-reflections view (typo fixing). KEEP SCOPE MINIMAL — the full Reflections revamp is Follow-Up Build G; do not add privacy columns, do not restructure the Past tab.

### 10. NEW-PPP (row 222 · bug `85785cfb`) — VERIFY ONLY
Founder believes the stale "Other"-bucket tasks on Play Ruthie's dashboard were already removed. Open View-As → Ruthie (Play), confirm the Other segment is clean. If clean: report for closure. If stale tasks persist: investigate what populates the Play "Other" segment (`task_segments` grouping + recurring-task visibility / `get_member_day_obligations` Convention #271) — but ASK the founder before building anything beyond a targeted fix.

## Close-out requirements
- `tsc -b` clean; `npm run lint` clean of new errors
- Per-item verification table (Wired/Stubbed/Missing + how verified, desktop + 375px for UI items)
- Append a dated note to each bug report's `admin_notes` (IDs above) describing the fix + commit SHA — do not change `status`
- Annotate worksheet rows 207/209/210/212/213/214/211/223 + row 31 in `claude/web-sync/TRIAGE_WORKSHEET.md` (annotate, don't flip founder-decision locks)
- Update `WIRING_STATUS.md` if any wiring state changed; `STUB_REGISTRY.md` if stubs created
- Logical commits per fix or small groups; push at the end
