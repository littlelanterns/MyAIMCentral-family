# PRD-12A LifeLantern + PRD-12B Family Vision Quest — Shared Recon Brief (Sonnet reader, 2026-07-04)

> Archived by the dispatch factory (condensed, citations kept). Consumed by `PRD12A.md` + `PRD12B.md`.

## A. Scope (separate inventories)

**PRD-12A (731 lines, Not Started).** 13 life areas (spiritual_faith … fun_recreation_rest; §70-92; section_key TEXT, custom areas post-MVP). 6 screens: Hub (area cards, status, staleness "Check in?", vision preview, Generate Goals, drag-reorder; §114-146); Area Detail (two-panel Assessment/Vision, Update-with-LiLa vs Edit-Directly, snapshot history, auto Gap Summary, prev/next; §148-196); LiLa Assessment convo (5-step, references InnerWorkings, archives prior content to snapshot; §198-222); Vision Casting convo (2-4 role models/area, trait extraction; §224-250); Personal Vision Statement (synthesized narrative, regenerate, version history, visibility private/shared + member picker, is_included_in_ai; §252-277); Goal Generation (gap-driven; routes: Guiding Star direct / Best Intention direct / Task routed / Tracker routed / complex→Project Planner stub; §279-320). AI: mode `life_lantern`, sonnet, context_sources [self_knowledge, guiding_stars, best_intentions, life_lantern], roles ['mom'] (expand adult/independent at Full Magic), 5 LiLa roles incl. Organic Check-in Partner (staleness nudge in Sitting LiLa, no notifications); voice = standard Whisper. 4 tables: life_lantern_areas (unique member+section_key), life_lantern_area_snapshots (append-only), life_lantern_role_models, personal_vision_statements (append-only, is_active trigger, visibility, area_snapshot JSONB). Permissions: mom full + sees all; dad own gated; teen youth-lite POST-MVP/Full Magic; Guided/Play/SA absent. Keys: life_lantern, life_lantern_voice, life_lantern_teen. 7 stubs (Project Planner dest, youth-lite, custom areas, VR filter, rhythm integration, dashboard widget, cadence config).

**PRD-12B (645 lines, Not Started).** 11 vision sections (home_feels … work_business_creating; §61-80). 7-8 screens: Quest Hub (mom portal, progress per section, Our Vision/Progress/History tabs; §87-107); Quest Setup (section toggles, per-section member picker w/ age defaults, ONE in_progress quest per family; §110-128); Section View (mom-only aggregated response cards, By Section/By Person; §131-156); Answer Input (role-adaptive: Independent/Adult full + lightweight 2-4Q LiLa help + voice; Guided simplified; Play mom-captures, captured_by_mom flag; §159-185); Section Discussion — Facilitated (LiLa theme summary NEVER quoting individuals; **real-time CHUNKED Whisper transcription — rolling chunks, explicit anti-timeout design**; edit transcript; mode column async|live, live = stub; §187-209); Section Vision Editor (Regenerate with LiLa, history, finalize; §211-227); Family Vision Statement (sections are truth, statement generated fresh; §229-250); Family Guiding Stars Generation (suggested cards w/ section attribution → guiding_stars owner_type='family'; §252-269). AI: mode `family_vision_quest`, sonnet, person_selector=false, roles ['mom']; "we"-language, never quotes individuals, vision-not-policy. 6 tables: family_vision_quests, vision_sections (unique family+section_key), vision_section_history, vision_section_responses (responding/captured_by, voice_recording_url, lila_assisted), vision_section_discussions (lila_summary, audio_recording_url, transcript, transcript_chunks JSONB), family_vision_statements (append-only, is_active). Permissions: mom sees ALL; dad/teen assigned sections only, NEVER others' answers (anti-comparison, mom-only aggregation); Guided simplified; Play mom-captures; SA optional. Keys: family_vision_quest, family_vision_discussion, family_vision_guiding_stars, family_vision_live_mode. 6 stubs (live mode, VR filter, Family Celebration filter, custom sections, Family Mission Statement, section visibility).

## B. What exists — **both 0% built** (BUILD_STATUS:57 Phase 22 Pending; deps 07/08/13 all satisfied)

- All 10 tables: absent (live_schema + migrations, zero hits). No triggers, no RLS.
- Guided modes REGISTERED (migration 000007:239-240; parent_mode set in 000013:129) pointing at nonexistent tables; **real per-mode prompts just landed via SAFETY-BETA-GATE Slice A** (`lila-chat/index.ts:120-125`, safety-preamble imported at :10) — conversation shell exists, everything behind it empty; no UI reaches the modes.
- context-assembler.ts has only 3 loaders (BookShelf :458, FilteredArchive :544, Wishlist :657) — **Convention #57's "7 stub loaders" text is STALE**; no LifeLantern/vision scaffolding exists; loaders are net-new (follow BookShelf/Archive pattern).
- `/life-lantern` route exists (App.tsx:155) → PlaceholderPage w/ full PlannedExpansionCard (registry entry present). **`/family-vision` route does NOT exist at all**; `family_vision_quest` registry entry is orphaned (never rendered).
- Sidebar keys DRIFTED: `lifelantern` (mom/dad, Sidebar.tsx:206,291) vs `life_lantern` (placeholder/registry/mode) vs `life_lantern_teen` (:357). **Zero keys registered in feature_key_registry** (all 7 across both PRDs).
- feature_expansion_registry.ts:40-52 + lanterns-path-data.ts:659-669 both say "six life areas" w/ names not matching the PRD's 13 — marketing-copy drift.
- Family Hub `family_vision` section: registered slot, `return null` (FamilyHub.tsx:41-44,111-112; useFamilyHubConfig.ts:36). Archives stub cards: FamilyOverviewDetail.tsx:688-695, MemberArchiveDetail.tsx:1032-1047.
- Rhythms Quarterly Inventory: fully wired stub per Convention #178 (QuarterlyInventoryCard.tsx:1-14,83-99; trigger_type lifelantern_staleness seeded across 11 migrations).
- `guiding_stars.owner_type` schema-ready for family values; zero family-GS UI (STUB rows 154/162). **`family_best_intentions` is NOT a 12B dependency** — separate table already shipped via PRD-14D.
- **Chunked Whisper does NOT exist** — whisper-transcribe is single-shot; both PRDs cross-reference the chunked infra as if the other built it (12A §388 borrows from 12B; 12B treats it as anticipated). Net-new work.
- Client helper `src/lib/ai/context-assembly.ts:37-41,53` already includes life_lantern/family_vision page contexts + mode arrays — live but unreachable (not stale code).
- **STUB_REGISTRY:185 "Complex goal → Project Planner | PRD-12A | PRD-29 | ✅ Wired | Phase 33" is FALSE** — BigPlans unbuilt (BUILD_STATUS:68 Pending), zero goal_decomposition writer code; the row describes the PRD-29 addendum's intended contract (addendum §108-135) as shipped. Needs correction. (Sibling row 186 was corrected; 185 was missed.)

## C. Schema gaps
Total absence — all 10 tables, both is_active triggers, all RLS. Check `scripts/full-schema-dump.cjs` DOMAIN_ORDER knows these domains at build (probably doesn't).

## D. Waiting-on (citations)
Rhythms Quarterly stale-areas/quick-win (Conv #178); Hub family_vision null slot; Archives FO + member-detail stub cards (PRD-13 text commits to live-reference aggregation: PRD-13 §124,353,685-687,887-888); family-GS creation/render UI (PRD-06 §233,355,369,538,633-646,662); BigPlans complex-goal destination (double-deferred; PRD-29 addendum §108-135); PRD-11 LifeLantern victory filter (12A §609, 12B §544); PRD-14 dashboard widget (12A §611,651); PRD-16 meetings context (PRD-16 addendum :130-134); PRD-15 group-convo decision at build (:177-184); BookShelf SemanticSearchPanel trigger (PRD-23 Session Addendum :208); Daily-Progress Path I toggle inheritance on goal-generated tasks (STUB:536); mode context_sources need real loaders; chunked Whisper infra (12B only); Permission-Matrix teen tiers (PRD-31 matrix :106,151,161 — Maximum-only teen, consistent w/ 12A text).

## E. Conflicts (named)
1. Context-layer placement unspecified (PRDs predate 3-layer pipeline); client identity-regex already fires on vision|purpose|legacy → double-load/ambiguity risk with a new LifeLantern topic trigger.
2. Convention #248 category-1 conformance: modes declare context_sources → MUST use assembleContext; today generic chat w/ no grounding/persistence — "shell exists ≠ mostly done."
3. HITM alignment fine in spirit; re-verify against the CURRENT <HumanInTheMix> API at build.
4. Safe Harbor moot; BUT emotional/mental-health assessment convos need crisis coverage confirmed (safety-preamble now global — verify at build).
5. Celebration-only: no conflict (explicitly non-clinical, no scales).
6. Voice infra claim mismatch (chunked = net-new; see B).
7. Tier text consistent w/ Permission Matrix (teen Maximum-only); all TBD during beta.
8. Three live feature-key spellings, none registered — naming decision BEFORE useCanAccess wiring.
9. STUB:185 false-Wired row (see B).

## F. Open questions (absorbed into packs)
1. Build together vs split (12B separable; carries the chunked-Whisper risk). 2. Canonical key naming. 3. STUB:185 correction timing. 4. Complex-goal destination now (Tasks fallback per original text) vs wait-for-BigPlans. 5. Marketing copy 6→13 areas fix. 6. Teen expansion timing. 7. Live facilitated mode stays stub?
