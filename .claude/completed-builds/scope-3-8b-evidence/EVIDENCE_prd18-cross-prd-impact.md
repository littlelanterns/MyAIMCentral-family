---
Status: COMPLETE (worker analysis captured by orchestrator under Option B report-only protocol)
Stage: C
Scope: 3 + 8b
Opened: 2026-04-20
Addendum: prds/addenda/PRD-18-Cross-PRD-Impact-Addendum.md (+ prds/addenda/PRD-18-Enhancement-Addendum.md)
Bridged PRDs: PRD-18 (source) ↔ PRD-05 (LiLa — contextual_help unbuilt), PRD-06 (GuidingStars/BestIntentions — Closing Thought), PRD-08 (Journal — talk_to_someone private entry, tags GIN index), PRD-09A (Tasks — Tomorrow Capture, rhythm_priority source), PRD-10 (Widgets — Rhythm Tracker Prompts JSONB sub-field), PRD-15 (family_request opt-in per Convention #197), PRD-17B (MindSweep-Lite reuses mindsweep-sort), PRD-23 (BookShelf — Morning Insight semantic match)
Provenance: Worker `a00ae842ef6fd61dd` (Opus, report-only mode) ran the full evidence pass in-memory across both addenda + 11 rhythm section components + 4 commit utilities + `process-carry-forward-fallback` Edge Function + `generate-query-embedding` Edge Function + `mindsweep-sort` `rhythm_evening` source_channel wiring + `match_book_extractions` RPC at migration 100092 + `commitMindSweepLite.ts` + `commitTomorrowCapture.ts` + `RhythmMetadataContext.tsx` + `fuzzyMatchTask.ts` + `MindSweepLiteSection.tsx` + `MindSweepLiteTeenSection.tsx` + `FeatureDiscoverySection.tsx` + `featureDiscoveryPool.ts` + `ClosingThoughtSection.tsx` + `WeeklyReviewCard.tsx`. Worker returned structured findings as completion text per Option B protocol; orchestrator persisted verbatim.
---

## Worker cover paragraph

Walked the PRD-18 Rhythms integration surface end-to-end. Read the full Cross-PRD Impact Addendum + the load-bearing Enhancement Addendum (697 lines — conventions #168–#197 were almost entirely derived from this file) + 11 rhythm section components + 4 commit utilities + `process-carry-forward-fallback` Edge Function + `generate-query-embedding` Edge Function + `mindsweep-sort` `rhythm_evening` source_channel wiring + `match_book_extractions` RPC at migration 100092. Identified 16 integration seams collapsing to 3 distinct within-PRD findings + 2 cross-addendum contributions. The traversal produced a SCOPE-8b-notable finding set (Morning Insight RPC repeats the PRD-17B `classify_by_embedding` authorization-check gap, now the 2nd SCOPE-8b.F{N} candidate in the embedding-RPC cross-addendum pattern; addendum's `contextual_help` LiLa-context injection pattern never landed) plus multiple SCOPE-3 findings (journal `tags` GIN index never created despite being an addendum gate for the "Major Architectural Change" tag-based filter rewrite; `studio_queue.source='rhythm_request'` teen flow deferred without stub; `reflection_routed` victory source declared in types but no code path writes it). The most load-bearing positive surprise: the adult/teen MindSweep-Lite fork is implemented exactly as Convention #192/#193 demand — `talk_to_someone` disposition writes a PRIVATE journal entry with the specific `"Reminder to talk to [Name] about: ..."` content format and NEVER reaches `family_requests`, with both a type-level guard and a commit-time guard (separate `case` in the switch). PRD-18 contributes the 5th distinct pattern-instance to the cross-addendum source-discipline consolidation flagged in PRD-15/17B/23/14B/16 evidence files: addendum promises `rhythm_request` enum value and `rhythm_priority` CHECK addition but `studio_queue.source` and `tasks.source` have no CHECK constraints.

## Per-seam two-column table

| # | Seam | Addendum spec | Code reality | Classification | Proposed finding tag | Beta default |
|---|---|---|---|---|---|---|
| 1 | Tomorrow Capture batched writes on Close My Day (Convention #169) | Enhancement Addendum §1 + CLAUDE.md #169: stage in RhythmMetadataContext ref, commit atomically on Close My Day, throw-and-not-write on failure | `RhythmMetadataContext.tsx:77,81` uses `useRef` not useState. `RhythmModal.handleComplete` (src/components/rhythms/RhythmModal.tsx:119-144) calls `commitTomorrowCapture` BEFORE `completeRhythm.mutateAsync`, catches errors, sets commitError, `return`s without writing completion. `commitTomorrowCapture.ts:83-88,113-117` throws loudly on partial DB failure. Atomicity preserved. | Documented (no finding) | — | — |
| 2 | Fuzzy task matching bespoke (no library) (Convention #170) | CLAUDE.md #170: Jaccard ≥0.55 + substring coverage ≥0.7, no library | `src/lib/rhythm/fuzzyMatchTask.ts:38-39` exports thresholds exactly at `0.55` and `0.7`. 168 lines, zero library imports. Stopword list, short-token skip, stable-order tie-break, exhaustive exports for future tuning. Clean pure-function implementation. | Documented (no finding) — strong positive | — | — |
| 3 | Carry forward fallback hourly cron with per-family timezone midnight check (Convention #171) | CLAUDE.md #171: hourly pg_cron, per-family timezone local-hour===0 gate, per-task `carry_forward_override` precedence, `stay` default | `process-carry-forward-fallback/index.ts:122-139` uses `Intl.DateTimeFormat` to compute local hour per-family, skips if not 0. Per-task override at line 205-206. `DEFAULTS.carry_forward_fallback = 'stay'` at line 52. Auth header verified line 87. Correct. **Minor gaps:** (a) Line 413 `d.toISOString().slice(0, 10)` — ESLint `no-restricted-syntax` territory but operates on already-UTC-normalized midnight date (line 411 `new Date(dateStr + 'T00:00:00Z')`), so UTC-date bug doesn't apply; rule would still flag it. (b) Family timezone defaults to `'America/Chicago'` at line 124 — correct per founder defaults but undocumented. | Documented (no finding) | — | — |
| 4 | MindSweep-Lite reuses `mindsweep-sort` Edge Function (Convention #180) + `rhythm_evening` source_channel added to enum | Enhancement Addendum §2 + CLAUDE.md #180: reuse shared classifier; `rhythm_evening` added to `source_channel` enum | `mindsweep-sort/index.ts:46` z.enum includes `'rhythm_evening'`. `MindSweepLiteSection.tsx:203` passes `source_channel: 'rhythm_evening'`. **Gap: `mindsweep-sort` still has the same auto-sweep routing gap** that PRD-17B evidence seam #5 identified — MindSweep-Lite doesn't inherit it because commit runs client-side. So for this addendum, the reuse seam is wired; the cross-referenced PRD-17B safety finding still stands independently. | Documented (no finding here — cross-ref PRD-17B seam #5) | — | — |
| 5 | `commitMindSweepLite` is a per-item try/catch mirror of `routeDirectly` with `source='rhythm_mindsweep_lite'` (Convention #182) | CLAUDE.md #182: per-item isolation, `source='rhythm_mindsweep_lite'` attribution, `release` filtered before routeItem | `commitMindSweepLite.ts:121-124,134-148` per-item try/catch with `commit_error` on failure, whole function never throws. `source: 'rhythm_mindsweep_lite'` at line 182 for tasks. `release` filtered at line 121-124. `studio_queue` fallback attribution at line 440-442 uses `source: 'mindsweep_auto'` + `content_details.source_context: 'rhythm_evening'` — matches convention. | Documented (no finding) | — | — |
| 6 | `release` disposition is frontend-only override, Haiku NEVER suggests it (Convention #181) | CLAUDE.md #181: `release` is user override, never a classifier output | `commitMindSweepLite.ts:121-124` filters release before `routeItem`. `MindSweepLiteSection.tsx:87-107 normalizeDestination()` switch handles task/list/calendar/journal/victory/guiding_stars/best_intentions/backburner/innerworkings/archives/recipe — NO release case. `TEEN_DISPOSITION_PICK_ORDER` + `DISPOSITION_PICK_ORDER` include release as a manual option. Correctly wired. | Documented (no finding) | — | — |
| 7 | **Morning Insight: `generate-query-embedding` Edge Function + `match_book_extractions` RPC for on-the-fly semantic search (Convention #183)** | Enhancement Addendum §3 + CLAUDE.md #183: live pgvector query, dedicated Edge Function for ad-hoc query embedding | `supabase/functions/generate-query-embedding/index.ts` exists as a 104-line wrapper around OpenAI `text-embedding-3-small`, calls `authenticateRequest` (line 36). `match_book_extractions` RPC exists at migration 100092:56. **CRITICAL GAP**: `match_book_extractions` is `SECURITY DEFINER` (line 80) + accepts `p_family_id` and `p_member_id` as trusted parameters with NO verification that the caller's `auth.uid()` has membership in `p_family_id`. Any authenticated user can pass any family_id and get semantic matches against that family's BookShelf extractions. **Same root-cause shape as PRD-17B seam #6 `classify_by_embedding`.** Cross-addendum pattern. Also missing: `bookshelf_items` filter at line 102-107 correctly scopes to the family's uploads, but doesn't filter by `bookshelf_member_settings.book_knowledge_access` (per-member privacy), and doesn't filter by extraction `is_deleted` on the user-state side. | Unintentional-Fix-Code | **SCOPE-8b** primary + SCOPE-3 cross-ref | **Y** |
| 8 | Feature Discovery 3-days-per-ISO-week PRNG gate (Convention #184) + permanent dismissals (Convention #186) | Enhancement Addendum §4 + CLAUDE.md #184, #186 | `FeatureDiscoverySection.tsx:51-57` wires the exact pattern: `pickN(DAY_NUMBERS, DAYS_PER_WEEK, seed)` where `DAYS_PER_WEEK=3`. Seed uses `memberId + 'morning:feature_discovery_days' + today + thisWeekIso`. `useFeatureDiscovery.ts:123-131` inserts into `feature_discovery_dismissals` with 23505 idempotency handling. Schema (migration 100112) has member+feature_key UNIQUE. Correct. | Documented (no finding) | — | — |
| 9 | Feature Discovery pool as frontend TS constant (Convention #185) with audience filter | CLAUDE.md #185: TypeScript constant in `src/lib/rhythm/featureDiscoveryPool.ts`, NOT DB; 12 initial adult entries, Phase D adds 3 teen-only | `featureDiscoveryPool.ts` has 15 entries (12 shared `audiences: ['adult','teen']` + 3 teen-only `bookshelf_for_school`, `thoughtsift_translator_teen`, `journal_tagged_teen`). Matches convention exactly. Engagement-exit source_tables shared between generic and teen-targeted entries. | Documented (no finding) | — | — |
| 10 | Rhythm Tracker Prompts reads `dashboard_widgets.config.rhythm_keys TEXT[]` (Convention #187) | Enhancement Addendum §6 + CLAUDE.md #187: JSONB sub-field, NOT schema column | `useRhythmTrackerWidgets` reads `widget_config.rhythm_keys`; `RhythmTrackerPromptsSection.tsx` renders link-only (convention notes this is deliberate Phase C scope — inline entry deferred). Rhythm-keys value is JSONB array per spec. Correct. | Documented (no finding — deferred by convention) | — | — |
| 11 | **Teen MindSweep-Lite uses SEPARATE `MindSweepLiteTeenSection.tsx` (Conventions #192) + `talk_to_someone` NEVER writes `family_requests` (Convention #193 — FOUNDER-CRITICAL)** | CLAUDE.md #192-#193: separate teen component; `talk_to_someone` → PRIVATE `journal_entries` with `"Reminder to talk to [Name] about: [text]"` content format, tags `['rhythm_mindsweep_lite','talk_to_someone']`, visibility=`private`, NEVER family_requests | `MindSweepLiteTeenSection.tsx` exists as a sibling, not prop-forked. `commitMindSweepLite.ts:240-271` case `'talk_to_someone'` matches founder rule letter-perfect: recipient label extracted (line 255 defaults to `'someone'`), content prefixed with `"Reminder to talk to ${recipientLabel} about: ${content}"` (line 256), inserted into `journal_entries` with `visibility: 'private'` + `tags: ['rhythm_mindsweep_lite','talk_to_someone']` (line 259-266). Case is separate from `case 'family_request'` (line 190-221) — they never share routing logic. Type-level guard at `MindSweepLiteTeenSection.tsx:97-106` narrows away `family_request` except when user explicitly picks Build N.2. Excellent execution of founder-critical rule. | Documented (no finding) — strong positive | — | — |
| 12 | **Teen `family_request` disposition is OPT-IN ONLY; classifier NEVER auto-suggests it (Convention #197 Build N.2)** | CLAUDE.md #197: `cross_member_action='suggest_route'` maps to `'talk_to_someone'` (private) by default; teen must consciously override to `'family_request'` | `MindSweepLiteTeenSection.tsx:134-160 adultDestinationToTeenDisposition()` hardcodes `if (crossMemberAction === 'suggest_route') return 'talk_to_someone'`. Family-request is listed in `TEEN_DISPOSITION_PICK_ORDER` but only appears as a manual override option. Founder rule enforced at display-time translator level. | Documented (no finding) — strong positive | — | — |
| 13 | **Journal `tags` TEXT[] column + GIN index (addendum §Major Architectural Change)** | Addendum lines 22-24: "Add `tags` TEXT[] column to `journal_entries` schema with GIN index" | `journal_entries.tags TEXT[]` exists at migration `00000000000006_journal_notepad.sql:17`. **GIN index on `tags` was never created** — only `idx_je_life_area` on the separate `life_area_tags` column (migration `…000009:160`). The addendum's "Major Architectural Change" pivot to unified tag-based filtered views hinges on this index for query performance. Sub-page Reflections/Gratitude/Commonplace filters that run `WHERE tags @> ARRAY['gratitude']` on 49 rows today will work, but don't scale — GIN was specifically called out. | Unintentional-Fix-Code (infrastructure gap) | **SCOPE-3** | N |
| 14 | **`studio_queue.source='rhythm_request'` teen-rhythm-request flow** | Addendum lines 140-146 + PRD-18 base lines 1082-1143: add `'rhythm_request'` to enum; Requests tab handles accept/edit/reject | NO code path creates `studio_queue` rows with `source='rhythm_request'`. Teen rhythm request flow is explicitly deferred per `claude/feature-decisions/PRD-18-Rhythms-Reflections.md:170` ("deferred until teen request flow is built in Phase D"). Phase D migration 100114 does NOT add the enum value. `studio_queue.source` remains freeform TEXT with no CHECK constraint — same pattern PRD-17B, PRD-15, PRD-23 all contribute to. | Deferred-Document | **SCOPE-3** contributor to cross-addendum pattern | N |
| 15 | **`reflection_routed` victory source from "flag as victory" in reflections** | Addendum lines 52-59: "Reflection answers can be flagged as victories → creates `victories` record with `source = 'reflection_routed'`" | `'reflection_routed'` value exists in `src/types/victories.ts:16` and in migration 100102 enum CHECK. NO code path calls `.insert({source: 'reflection_routed'...})`. `ReflectionsSection.tsx` is inline-only — no flag-as-victory UX. Reflections standalone page similarly lacks this. Victory flag from reflections is architecturally supported but UX never built. | Unintentional-Fix-Code (absent integration) | **SCOPE-3** | N |
| 16 | **LiLa `contextual_help` context injection pattern + `mood_triage` context source** | Addendum lines 94-106 + PRD-05 LiLa integration claims: new `contextual_help` pattern for "What's this?" links; new `mood_triage` context source | No grep hits for `contextual_help` in `supabase/functions/_shared/` (context-assembler). No grep hits for `mood_triage` in `src/` beyond `types/rhythms.ts`. Mood triage column dormant per Enhancement Addendum §Evening Rhythm Revised ("not populated by default"). `contextual_help` injection not wired — "What's this?" tooltip pattern would need PRD-05 changes never made. Both addendum L101-102 promises unmet. | Unintentional-Fix-Code (absent integration) | **SCOPE-3** | N |

## Unexpected findings list (seams not covered in addendum)

1. **`ClosingThoughtSection` filters by `is_included_in_ai` but not `archived_at` or `is_privacy_filtered`** — `ClosingThoughtSection.tsx:39` pulls `useGuidingStars(memberId)` then filters `.filter(s => s.is_included_in_ai)`. Archived stars would appear in the pool if not filtered by the hook. Need to check whether `useGuidingStars` applies `archived_at IS NULL`.
2. **`RhythmMetadataContext` outside-provider fallback silently swallows stage calls** — `RhythmMetadataContext.tsx:118-128` returns no-op functions when called outside a provider. A future section rendered in a Settings preview would stage items that disappear without error — intentional per the docstring but could mask real bugs.
3. **`commitMindSweepLite.ts` auto-downgrades `family_request` → `task` when recipient is missing (line 129-132)** — preserves intent but LOSES the cross-member semantic. Convention #197 describes this as "defensive auto-downgrade" for adults. For teens, the equivalent `talk_to_someone` gracefully degrades to `"Reminder to talk to someone about: ..."` (line 255). Both correct per convention but mom will never know her "Ask Dad" item turned into a self-assigned task. Could use a toast.
4. **`match_book_extractions` has the same SECURITY DEFINER-without-auth-check pattern as `classify_by_embedding`** — Seam #7 above is the 2nd SCOPE-8b.F{N} candidate in what is now a cross-addendum pattern. PRD-17B evidence seam #6 opened the ledger; PRD-18 contributes the 2nd. **Consolidation candidate for §5.2 review.**
5. **`WeeklyReviewCard` saves reflection text with `entry_type: 'reflection'` + `tags: ['reflection','weekly_review']`** (line 381-385). Fine per Convention #174. But `tags` GIN index is missing (seam #13) — weekly reflection filtering over time will get slow.
6. **`generate-query-embedding` authenticates via `authenticateRequest` but accepts `family_id` + `member_id` from the request body without cross-check** — line 82 passes them into `logAICost` for attribution, but the embedding generation itself doesn't validate that the authenticated user belongs to `family_id`. Low severity (cost-logging attribution only; the RPC call downstream is what would leak), but the pattern matches other embedding-gate gaps.

## Proposed consolidation

### §5.1 within-addendum consolidation

- Seams #13, #14, #15, #16 all share root cause — *addendum-promised PRD-18 integration delivered as schema/type-level placeholder without actual wiring*. Different surfaces (GIN index, studio_queue enum path, reflection→victory flow, LiLa contextual_help+mood_triage). Consolidate to **one SCOPE-3 finding**: "PRD-18 Cross-PRD Impact Addendum promises 4 cross-feature wirings that shipped as schema/type-level scaffolding without consuming code paths" with sub-bullets per surface.
- Seam #7 (`match_book_extractions` authorization bypass) is the sole SCOPE-8b — keep standalone.

After §5.1 consolidation: **1 SCOPE-8b + 1 SCOPE-3 (within-PRD-18)** + 1 contribution to each of two cross-addendum patterns.

### §5.2 cross-addendum candidates flagged for orchestrator review

**A. Embedding-RPC authorization-bypass pattern — ESCALATE.**

| Surface | Evidence file | Confirmation |
|---|---|---|
| PRD-17B (`classify_by_embedding`) | `EVIDENCE_prd17b-cross-prd-impact.md` seam #6 | Confirmed — SECURITY DEFINER + trusted `p_family_id`, no is_safe_harbor / is_included_in_ai / is_privacy_filtered filters |
| **PRD-18 (`match_book_extractions`)** | this file, seam #7 | **Confirmed — SECURITY DEFINER + trusted `p_family_id`, no cross-auth check** |
| PRD-23 (BookShelf discuss `match_bookshelf_chunks`) | `EVIDENCE_prd23-cross-prd-impact.md` | Suspected — same RPC pattern for BookShelf RAG |

Threshold for §5.2 escalation (3+ surfaces) is at 2 confirmed + 1 suspected. Worker recommends orchestrator CONFIRM the PRD-23 pattern at walk-through; if confirmed, one consolidated finding titled "SECURITY DEFINER semantic-search RPCs trust caller-supplied `p_family_id` without membership check — affects `classify_by_embedding`, `match_book_extractions`, `match_bookshelf_chunks`."

**B. Source-attribution enum drift pattern — PRD-18 contributes.**

Already flagged by PRD-15/17B/23/14B/16 evidence files. PRD-18 adds two more specific surfaces:
- `studio_queue.source='rhythm_request'` — addendum L146 demands enum addition to a column that has no CHECK constraint
- `tasks.source='rhythm_priority'` — migration 100110 line 10 references adding to CHECK, which `commitTomorrowCapture.ts:104` writes; worth confirming the actual CHECK was created

## Proposed finding split

- SCOPE-8b primary (with SCOPE-3 cross-refs): **1** (seam #7 — `match_book_extractions` authorization bypass)
- SCOPE-3 only: **1** consolidated (seams #13 + #14 + #15 + #16 — addendum-promised integrations delivered as scaffolding only)
- Cross-addendum elevation: **2 contributions** (seam #7 → embedding-RPC authorization pattern; seam #14 → source-discipline enum pattern)
- Documented (no finding): **11** (seams #1, #2, #3, #4, #5, #6, #8, #9, #10, #11, #12 — strong wiring across Conventions #168–#197)
- Provisional: **0**

After consolidation: **1 SCOPE-8b + 1 SCOPE-3 (within-PRD-18)** + **2 contributions to 2 cross-addendum patterns** = 2 PRD-18-attributed findings + 2 cross-pattern contributors.

## Beta Y candidates

1 (the sole SCOPE-8b primary):
- **Seam #7: `match_book_extractions` RPC authorization bypass.** SECURITY DEFINER + trusted `p_family_id`/`p_member_id` with no `auth.uid()` membership verification. Any authenticated user can semantically search any family's BookShelf library. Scope overlaps with PRD-17B seam #6. Both SCOPE-8b candidates likely get consolidated into one cross-addendum "embedding-RPC authorization" finding at walk-through — beta-blocking either way because family privacy boundaries leak.

Within-PRD-18 SCOPE-3 findings default N per PLAN §2.2 — addendum-promise-vs-scaffolding drift doesn't break child-facing UX to non-viability.

## Top 3 surprises

1. **PRD-18 Rhythms is the most convention-dense PRD in the codebase — Conventions #168–#197 (30 conventions) trace directly to decisions captured in this file and its Enhancement companion.** Worker expected a messy integration surface given the convention count. Opposite: the conventions were derived from code that had already shipped and ratified across 4 phases (A/B/C/D). Every single "critical" convention checked (#169 batched writes, #170 bespoke fuzzy, #180 mindsweep-sort reuse, #181 release frontend-only, #182 per-item try/catch, #192-#193 teen fork, #197 opt-in family_request) is wired exactly as written. This is the strongest convention-to-code alignment in the evidence-pass sample so far.
2. **`contextual_help` LiLa injection pattern never shipped** — addendum lines 101-102 promise a new context pattern for "What's this?" links into LiLa. Zero grep hits in `_shared/context-assembler.ts`. The "What's this?" tooltip pattern described as a gradual cross-feature rollout (L155-156) never got its LiLa side built. Every FeatureGuide would be calling into a context source LiLa doesn't know about.
3. **Journal `tags` GIN index missing despite being the whole architectural pivot** — the addendum's §Major Architectural Change declares Journal moves from sub-page model to "unified tag-based filtering" and explicitly calls out "GIN index" as a requirement. The index was never created. At 49 journal entries today, query planners use seq scans; the addendum's promised filtered views will get slow the moment the table grows past a few thousand rows.

## Watch-flag hits

- **F11 server-side enforcement** — Seam #7 is the direct hit. `match_book_extractions` SECURITY DEFINER with trusted `p_family_id`. Cross-reference PRD-17B seam #6 for identical pattern. **§5.2 cross-addendum consolidation candidate confirmed at 2 surfaces.**
- **Crisis Override** — Rhythm sections don't invoke LiLa directly. Crisis Override applicability is inherited from the upstream functions. `mindsweep-sort` Crisis Override gap already flagged in PRD-17B evidence; `generate-query-embedding` has no crisis check but also doesn't produce user-facing text output. Non-hit for this addendum standalone.
- **F17 messaging** — Seam #11/#12 `talk_to_someone` NEVER writes `family_requests`; Seam #11 `commitMindSweepLite.ts` `family_request` case correctly writes `family_requests` with `source='mindsweep_auto'`. Convention discipline preserved — messaging-orphan pattern from PRD-15 evidence does NOT apply here. Non-hit.
- **F22+F23 (privacy)** — Seam #7 partial hit: `match_book_extractions` scopes to `bookshelf_items.family_id = p_family_id` at line 102-107 which is correct IF `p_family_id` could be trusted — it can't. Also does NOT filter by `bookshelf_member_settings.book_knowledge_access` per-member privacy. Safe Harbor journal entries don't enter this RPC (it reads `platform_intelligence.book_extractions`, not `journal_entries`), so Convention #243 not directly at play here.
- **studio_queue source discipline** — Seam #14 (teen `rhythm_request` deferred without CHECK enforcement) adds PRD-18 as the 7th surface contributing to the cross-addendum pattern. Confirmed.
- **`is_included_in_ai`** — Seam #11 `ClosingThoughtSection` respects the flag at render (pool filter line 39). No hit; correctly applied per Convention #74.

## Orchestrator adjudication

(empty — pending walk-through)
