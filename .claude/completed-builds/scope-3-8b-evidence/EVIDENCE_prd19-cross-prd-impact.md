---
Status: COMPLETE (worker analysis captured by orchestrator under Option B report-only protocol)
Stage: C
Scope: 3 + 8b
Opened: 2026-04-20
Addendum: prds/addenda/PRD-19-Cross-PRD-Impact-Addendum.md
Bridged PRDs: PRD-19 (source) ↔ PRD-13 (Archives — host for all column extensions), PRD-15 (HTRM context in messaging coaching), PRD-21 (relationship tools consume private_notes + relationship_notes), PRD-28B (Compliance reports — Deferred-to-Gate-4), PRD-30 (Safety pattern summaries — Deferred), PRD-34 (Mediator Full Picture consumes relationship_notes)
Provenance: Worker `a8172a5a9ddb72197` (Opus, report-only mode) ran the full evidence pass in-memory across the addendum (168 lines) + full PRD-19 body (1180 lines, two passes) + live_schema.md rows for `private_notes` / `relationship_notes` / `archive_context_items` / `archive_member_settings` + Edge Function `_shared/relationship-context.ts` (681L) + `lila-mediator/index.ts` (`loadFullPictureContext`) + `_shared/context-assembler.ts` (three-tier toggle implementation at line 557-625) + `privacy-filter.ts` + `/archives` + `/people` placeholder page routes + migration `00000000000007_lila_ai_system.sql:247` (family_context_interview mode seed). Worker returned structured findings as completion text per Option B protocol; orchestrator persisted verbatim.
---

## Worker cover paragraph

Walked the PRD-19 Cross-PRD Impact Addendum (168 lines) end-to-end against code reality. Read the full addendum + the full PRD-19 body (1180 lines, two passes for schema/AI sections + flow/stubs) + live_schema.md rows for `private_notes` (exists, 0 rows, incomplete columns), `relationship_notes` (exists, 0 rows, incomplete columns), `archive_context_items` (no PRD-19 columns), `archive_member_settings` (has `display_name_aliases` only, no `external_alias`/`primary_alias`/`use_alias_*`). The four flagship PRD-19 tables that should exist (`member_documents`, `guided_interview_progress`, `monthly_data_aggregations`, `generated_reports`) are ALL ABSENT from live schema. Read Edge Function `_shared/relationship-context.ts` (681L) which queries `private_notes` + `relationship_notes` correctly for PRD-21 tools; `lila-mediator/index.ts` (`loadFullPictureContext`) queries both for Full Picture synthesis. Read `_shared/context-assembler.ts` which demonstrates the three-tier toggle enforcement correctly (`is_included_in_ai` at person → folder → item) + `applyPrivacyFilter()` server-side enforcement via `privacy-filter.ts`. Confirmed `/archives` and `/people` are both placeholder pages — no PRD-13/PRD-19 Archives UI exists at all. Cross-referenced Convention #95: "Mediator supersedes `relationship_mediation` (PRD-19). Mode key `relationship_mediation` was never created." Confirmed `family_context_interview` guided mode seeded in migration 00000000000007 (model_tier=sonnet contra PRD-19 spec saying haiku) but NO UI entry point. PRD-28B handoff (reports → compliance) correctly deferred-to-Gate-4. **Primary F22/F23 carry-forward direct hit: `archive_context_items` schema has ZERO of the PRD-19 columns. Report generation queries CANNOT exist yet because reports/aggregation pipeline is unbuilt — so the F22/F23 reports × archive column drift is latent, not materialized.** The three-tier toggle is implemented correctly where it runs; the PRD-19 sharing extensions on top of it do not exist. 10 seams collapsing to 4 distinct findings after §5.1.

## Per-seam two-column table

| # | Seam | Addendum spec | Code reality | Classification | Proposed finding tag | Beta default |
|---|---|---|---|---|---|---|
| 1 | **`archive_context_items` gets 4 new columns: `is_private_note`, `is_shared_with_spouse`, `sort_order`, `document_id`** + enum extensions `source += 'guided_interview','document_upload'` and `context_type += 'generated_report'` | Addendum L116-124 + base PRD L655-662 Schema Additions + Decisions 5/6 L1126-1127 + Convention L1092-1093 | `archive_context_items` live schema has: id, family_id, folder_id, member_id, context_field, context_value, context_type, is_included_in_ai, is_privacy_filtered, source, source_conversation_id, source_reference_id, added_by, usage_count, last_used_at, link_url, price_range, archived_at, created_at, updated_at, is_negative_preference. **ZERO of the PRD-19 additions present.** Grep confirms `is_private_note\|share_with_spouse\|share_with_family` **zero matches** across `supabase/migrations/` and `supabase/functions/`. The dual-context sharing model (PRD-19 flagship feature per Decisions 5 + 6) is unbuildable on current schema. | Unintentional-Fix-Code | **SCOPE-3** High | Y (archive sharing is a user-facing Archives detail view feature — without these columns the "Share with Spouse / Share with Family / Include in LiLa" 3-checkbox UI cannot exist, meaning cross-family-member context sharing is entirely missing) |
| 2 | **`archive_member_settings` gets alias columns**: `aliases TEXT[]`, `use_alias_in_external BOOLEAN`, `primary_alias TEXT` for external LLM privacy substitution + `display_name_aliases TEXT[]` for nickname recognition + `guided_interview_progress JSONB` | Addendum L117 + base PRD L646-654 + Nickname vs Alias convention (base PRD L718-734, Convention L1094-1095, Decision 14 L1135) | Live schema `archive_member_settings` has: id, family_id, member_id, is_included_in_ai, overview_card_content, overview_card_updated_at, created_at, updated_at, physical_description, reference_photos, `display_name_aliases`. **Only `display_name_aliases` exists (nicknames for recognition). Zero columns for external_alias / use_alias_in_external / primary_alias / use_alias_for_external.** Convention #79 (name detection) partially satisfied via `display_name_aliases` nickname recognition in `supabase/functions/_shared/relationship-context.ts:105-124`. **External-LLM alias substitution (PRD-05C Optimizer dependency) has no schema.** | Unintentional-Fix-Code | **SCOPE-3** Medium | N (Optimizer alias substitution is tier-gated; external LLM privacy surface deferred alongside full PRD-05C Optimizer build) |
| 3 | **`private_notes`, `relationship_notes` tables created with MVP column set** — `private_notes` needs `sort_order` + `archived_at`; `relationship_notes` needs `is_available_for_mediation BOOLEAN DEFAULT true` + `sort_order` + `archived_at` | Base PRD L502-570, Addendum L148-156, Decision 18 L1139 | Live schema `private_notes` has: id, family_id, about_member_id, author_id, content, is_included_in_ai, created_at, updated_at. **Missing `sort_order` + `archived_at`**. Live schema `relationship_notes` has: id, family_id, author_id, person_a_id, person_b_id, content, is_included_in_ai, created_at, updated_at. **Missing `is_available_for_mediation` + `sort_order` + `archived_at`**. `supabase/functions/lila-mediator/index.ts:174-191` `loadFullPictureContext` queries `relationship_notes` without any `is_available_for_mediation` filter — the opt-out per-note mediation exclusion specced in Decision 18 cannot function because the column doesn't exist. Also missing `archived_at` filter → soft-deleted notes would still be pulled. | Unintentional-Fix-Code | **SCOPE-8b** primary + **SCOPE-3** cross-ref | Y (missing `is_available_for_mediation` column means authors cannot opt a note out of Full Picture synthesis — a per-author privacy control documented as a user choice but unenforceable at query time. Trust-violation shape matches the PRD-14D PIN-bypass pattern.) |
| 4 | **4 PRD-19 tables absent entirely: `member_documents`, `guided_interview_progress`, `monthly_data_aggregations`, `generated_reports`** | Base PRD L477-641 Data Schema + MVP checklist L1028-1047 (11 Must-Have items depend on these 4 tables) + Decision 11 L1132 + Addendum L148-156 | Grep of `claude/live_schema.md` for any of the four table names returns **zero matches**. Grep of `src/` and `supabase/functions/` for these table names returns zero matches — no UI, no Edge Functions, no migrations. `src/pages/placeholder/index.tsx:30-32` — `/people` route IS a PlaceholderPage stub. `/archives` also a PlaceholderPage (L34-36). **The PRD-19 reports pipeline referenced as the F22/F23 seam DOES NOT EXIST** — which means the reports-vs-archive-column-drift carry-forward hasn't yet materialized in code (latent risk for when pipeline is built). | Unintentional-Fix-Code | **SCOPE-3** High | Y (Archives + People pages are placeholders — PRD-19 MVP checklist 24+ items all blocked. Archives is the family context surface LiLa claims to know from; without it, every LiLa tool operates at ~30% context depth. Matches the single worst unbuilt-PRD shape in the platform.) |
| 5 | **Monthly context freshness review on billing day** — aggregation → compare → suggest → mom review. Safe Harbor aggregation-exclusion guardrail (CLAUDE.md #243) applies | Base PRD L886-919 four-step process + Convention #243 | No cron job, no Edge Function `monthly-aggregate`, no billing-day trigger, no `process-monthly-aggregate` RPC. `lila_conversations.is_safe_harbor` column exists + RLS excludes Safe Harbor on parent read. **But Convention #243 grep-based CI check and RLS/view guard is explicitly "convention text only at this stage".** When PRD-19 aggregation is built, it MUST add `is_safe_harbor=false` filter — currently no safety net. | Deferred-Document | — (latent — no Scope 3/8b finding yet since no code exists to fault) | — |
| 6 | **Full Picture Mediation: notes from all authors loaded with neutral labels** + safety exception + safety_triggered flag | Base PRD L746-816 + Addendum L17-18 + Decision 17 L1138 + Convention L1090 | `supabase/functions/lila-mediator/index.ts:166-214` `loadFullPictureContext` correctly uses neutral labels via `authorLabels` map, never attributes by name ✓. Safety trigger patterns + `SAFETY_TRIGGERED_RESPONSE` + `safety_triggered` flag persisted on `lila_conversations.context_snapshot` ✓ per PRD-34 Convention #96. But: (a) does NOT filter `is_available_for_mediation = true` (column doesn't exist per seam #3); (b) does NOT filter `archived_at IS NULL` on notes. These are downstream consequences of seam #3, not independent defects. | Cross-ref seam #3 | — | — |
| 7 | **Private notes + relationship notes loaded in general `lila-chat` context** | Base PRD L739-744 context assembly table | `supabase/functions/_shared/context-assembler.ts` — grep of the file shows `private_notes` and `relationship_notes` **zero matches** in the main `lila-chat` context assembler. They ARE loaded in `_shared/relationship-context.ts:164-180` (PRD-21 tools) and `lila-mediator/index.ts:174-205`. General LiLa chat does NOT pull them even when name-detection fires. User talks to general LiLa about a family member → private notes about that member are ignored. | Unintentional-Fix-Code | **SCOPE-3** Low | N (general chat is less-valuable than relationship tools for this context; documented gap but not trust-violating) |
| 8 | **`family_context_interview` guided mode registered** | Base PRD L705-716 + Addendum L69-78 + Convention L1096 | `supabase/migrations/00000000000007_lila_ai_system.sql:247` seeds the mode. **Drift: seeded as `sonnet` but PRD L710 spec says `haiku`** ("simple question presentation + answer parsing — not complex reasoning... `haiku` model tier reflects this lightweight usage"). No UI entry point exists; no Edge Function handles the mode beyond generic `lila-chat`. Also no `guided_interview_progress` table so progress tracking across 9 categories unimplemented. | Unintentional-Fix-Code | **SCOPE-3** Low | N (model tier drift means 12-20x higher cost per turn than specced — but no UI invokes it, so cost impact is $0 until Phase 20 UI lands. Cross-ref Scope 4.) |
| 9 | **Archives UI page** enhanced member detail, context overview drag-ordering, documents, reports, relationship notes, HTRM card, guided interview, aliases, partner sharing settings | Base PRD Screens 1-8 (L76-464) + MVP checklist L1024-1062 + Addendum Impact on PRD-13 L116 | `/archives` → `src/pages/placeholder/index.tsx:34-36` PlaceholderPage; `/people` → L30-32 PlaceholderPage. **Entire PRD-19 UI surface is unbuilt.** Grep of `src/` for `RelationshipNote\|PrivateNote\|GuidedInterview\|MonthlyReport\|FamilyContextInterview\|HowToReachMe` returns **zero files**. `src/lib/ai/context-assembly.ts:302-306` `loadPartnerContext` is a STUB returning empty array. | Deferred-Document (Phase 20 per stub comment) | — | — |
| 10 | **PRD-28B compliance handoff** | Base PRD L295 Forward note + L1001 Stubs + L1066 MVP when dependency ready | Not addressed in addendum (pre-dates PRD-28B). PRD-28B is DEFERRED-TO-GATE-4 per Round 0. Seam stops at the addendum boundary. | Deferred-Document | — | — |

## Unexpected findings list

1. **Model-tier drift on `family_context_interview` seed — `sonnet` in migration vs `haiku` in PRD specification** (seam #8). Cost pattern P9 implication: a fully-wired Phase 20 interview would cost ~12-20x specced per turn if left at sonnet. Orchestrator may want to cross-reference Scope 4 cost-pattern audit output.

2. **Naming drift across sharing-toggle column names by host table.** `best_intentions.is_shared_with_partner`, `guiding_stars.is_shared_with_partner` (exist in live schema) vs `archive_context_items.is_shared_with_spouse`/`share_with_family` (PRD-19 spec, not implemented). "Partner" vs "spouse" is the same concept; unifying the vocabulary matters if a future cross-table sharing reconciliation ever happens. The addendum itself at L99 calls out this inconsistency as an action item — and both sides of that consistency check have drifted in opposite ways.

3. **`family_context_interview` seeded guided mode with no UI entry point** is a cross-addendum pattern candidate: how many other guided modes are seeded but have no invocation path? Flag for orchestrator attention during synthesis pass.

4. **`loadFullPictureContext` in lila-mediator does not apply `archived_at IS NULL` to either `relationship_notes` or `private_notes` queries.** When soft-delete is eventually wired on those tables (seam #3), previously-archived notes will still leak into Full Picture synthesis. Fix is additive with the seam #3 migration.

5. **Convention #79 (name detection in context assembly) is partially satisfied for the nickname-recognition half but entirely unsatisfied for external alias substitution.** Nickname recognition wires via `display_name_aliases` in relationship-context.ts. External alias is Optimizer's concern (PRD-05C) and has no schema to read from.

## Proposed consolidation (§5.1 + §5.2 candidates)

**§5.1 within-addendum:** Seams #1, #2, #3 are all schema-level drift of the same shape — PRD-19 additions specced, migration not shipped. Consolidate to one finding: **"PRD-19 schema extensions across 4 tables are unimplemented."** Seam #4 (4 tables absent) is a different shape — entirely missing tables vs missing columns — keep separate. Seam #7 (general lila-chat doesn't load notes) is low-severity and stands alone. Seam #8 (model-tier drift) is cross-cuttingly interesting but low-severity alone; bundle with seam #9 as "PRD-19 Phase 20 unbuilt, these are residual drift items."

**§5.2 cross-addendum candidates flagged:**

- **Schema-extension-not-shipped pattern:** PRD-19 extends `archive_context_items` + `archive_member_settings` (this evidence) + several prior-addendum cases where PRDs spec additions to tables from earlier PRDs without shipping the migration. Pattern threshold at 3+ addenda may be reached — orchestrator watch.
- **`is_included_in_ai` three-tier toggle pattern — CONFIRMED correctly implemented in `_shared/context-assembler.ts:557-625` for Archives.** Person (archive_member_settings) → Folder (archive_folders) → Item (archive_context_items) + privacy_filter + archived_at. **This is the canonical correct implementation.** Unlike the drift noted in EVIDENCE_prd14 (dashboard_widgets.is_included_in_ai has no consumer) and EVIDENCE_prd14d (family_best_intentions.is_included_in_ai has no consumer), here the toggle chain works end-to-end. **Pattern status per watch-flag #6: Archives IS the reference implementation, other surfaces are the drift.**
- **"Documented opt-out / mom-configured accountability mechanism silently bypassed" pattern:** PRD-14D seam #5 PIN-per-intention tally + PRD-19 seam #3 `is_available_for_mediation` opt-out both fit this shape — spec documents user control, UI/schema partially captures it, runtime never enforces it. If a third case surfaces, elevate to cross-addendum SCOPE-8b finding.
- **`family_context_interview` registered but no UI:** potential cross-addendum pattern if other addenda reveal guided modes seeded without invocation paths. Flag for synthesis pass.

## Proposed finding split

After §5.1 consolidation:

- **F-A: PRD-19 sharing/privacy column extensions unimplemented across 4 tables** (seams #1, #2, #3 consolidated) — `archive_context_items` missing `is_private_note`, `is_shared_with_spouse`/`share_with_spouse`, `share_with_family`, `sort_order`; `archive_member_settings` missing `aliases`, `use_alias_in_external`, `primary_alias`; `private_notes` missing `sort_order`, `archived_at`; `relationship_notes` missing `is_available_for_mediation`, `sort_order`, `archived_at`. The 4-checkbox "Share with LiLa / Share with Spouse / Share with Family / Private note" model that Decisions 5+6 make foundational to Archives UX is unbuildable. The `is_available_for_mediation` flag is documented per-note user control that cannot be toggled because the column doesn't exist. **SCOPE-3 primary (schema-only defect)** + **SCOPE-8b cross-ref** (because `is_available_for_mediation` gap violates a documented per-author privacy control in mediation mode, matching the PIN-bypass PRD-14D pattern F-A). **Beta Y** on the SCOPE-8b side; **Beta N** on SCOPE-3 side (schema-only waits for Phase 20 Archives build).

- **F-B: PRD-19 four core tables absent — monthly aggregation + reports + documents + interview progress pipeline is entirely unbuilt** (seam #4) — `member_documents`, `guided_interview_progress`, `monthly_data_aggregations`, `generated_reports` do not exist in live schema. No `monthly-aggregate` Edge Function, no billing-day cron, no `/reports` page, no upload flow, no interview UI. 24+ MVP checklist items from PRD-19 blocked. `/archives` and `/people` both render PlaceholderPage. **This is the F22+F23 carry-forward landing point: the reports × archive column drift is latent — the column drift from F-A exists today, but the reports pipeline that would query archive_context_items doesn't exist yet. F22/F23 therefore is a "pre-seam" warning for the Phase 20 Archives build, not a currently-exploitable defect.** **SCOPE-3 High** (largest unbuilt-PRD in the platform by spec surface area). **Beta N** — PRD-19 was never claimed as beta-required; Phase 20 is post-beta. Cross-reference to Convention #243 (Safe Harbor aggregation-exclusion): when this pipeline IS built, the aggregation queries MUST filter `is_safe_harbor=false` — pre-register the requirement in the remediation notes.

- **F-C: General `lila-chat` context assembly does not load `private_notes` or `relationship_notes`** (seam #7) — PRD-19 L740-745 specifies these are context sources in the assembly pipeline; `_shared/context-assembler.ts` has zero references to either table. They are loaded only by PRD-21 relationship tools and PRD-34 mediator. A user talking to general LiLa about a family member whom they have private notes on → those notes are ignored, even when name-detection per Convention #79 fires. **SCOPE-3 Low.** Beta N. Folds into Phase 20 Archives build.

- **F-D: `family_context_interview` model-tier drift (sonnet vs haiku) + no UI entry point** (seams #8, #9 bundled) — migration seeds as sonnet, PRD spec requires haiku. No UI to invoke. Purely residual drift that resolves when Phase 20 ships the interview UI + corrects the seed. **SCOPE-3 Low.** Beta N. Cross-refer Scope 4 cost-pattern audit for model-tier drift.

## Beta Y candidates

- **F-A (SCOPE-8b side):** `is_available_for_mediation` per-author opt-out is documented and cannot be honored (column doesn't exist). Matches F-A/SCOPE-8b shape of PRD-14D F-A (documented accountability mechanism silently bypassed). If this is confirmed as Beta Y, the fix is: migration to add the 3 columns to `relationship_notes` + apply the filter in `lila-mediator/index.ts:178` — small remediation.

- **F-B schema gap is NOT Beta Y** because the aggregation pipeline is entirely unbuilt and the spec is explicitly post-MVP for the reports pipeline.

## Top 3 surprises

1. **Archives is the canonical three-tier toggle implementation that works end-to-end** — the reference any other context-source should mirror. `_shared/context-assembler.ts:557-625` demonstrates the model: Step 1 person-level toggle from `archive_member_settings.is_included_in_ai`, Step 2 folder-level from `archive_folders.is_included_in_ai`, Step 3 item-level from `archive_context_items.is_included_in_ai` + `applyPrivacyFilter()` role-asymmetric enforcement + `archived_at IS NULL`. This is the first surface I've walked where the three-tier chain is fully wired correctly on the hot path. Prior drift findings (PRD-14 widget consumers, PRD-14D family_best_intentions) are the anomaly, not Archives.

2. **The F22/F23 carry-forward is latent, not materialized.** The reports pipeline is entirely unbuilt, so the predicted "reports query archive_context_items with old column assumptions" cannot yet be observed. The carry-forward becomes actionable as a **pre-seam warning** for the Phase 20 Archives build rather than as a current defect. Remediation is: when Phase 20 ships, the report-generation code MUST include the new PRD-19 columns (is_private_note filter, share_with_spouse filter) in its queries from day 1.

3. **`family_context_interview` guided mode is seeded as sonnet but spec calls for haiku** — a cost-pattern drift that's invisible today because no UI invokes the mode. Discovered while tracing the guided interview seam. When Phase 20 lands, if nobody fixes the seed, every interview turn will cost ~15-20x specced. Pre-registering this makes it a cheap fix at build time rather than a post-launch AI budget surprise.

## Watch-flag hits

1. **F11 server-side enforcement:** **HIT** — F-A cross-ref SCOPE-8b: `is_available_for_mediation` per-note opt-out is documented user control but unenforceable (column missing). Same server-side enforcement gap shape as PRD-14D PIN-bypass (EVIDENCE_prd14d F-A) and PRD-16 (prior evidence gaps). Cross-addendum pattern at 3+ now — flag for synthesis pass.
2. **Crisis Override:** **Not hit.** `lila-mediator` correctly integrates `detectCrisis` at L249-263 + safety trigger pattern matching at L31-45. Global Crisis Override convention (CLAUDE.md #7) honored.
3. **F17 messaging:** **Not applicable.** PRD-19 addendum L131 ("Impact on PRD-15") just flags that HTRM should be loaded by message coaching — not a messaging-pipeline defect.
4. **F22+F23 PRD-19 reports × archive column drift (THE PRIMARY CARRY-FORWARD):** **Latent hit, not materialized.** Reports pipeline unbuilt (F-B); archive column drift confirmed (F-A). The seam doesn't exist yet because the consumer doesn't exist. Pre-seam warning captured in F-B remediation note for Phase 20 build.
5. **studio_queue source discipline:** **Not applicable.** PRD-19 does not write to `studio_queue`.
6. **`is_included_in_ai` three-tier propagation (HIGH ATTENTION):** **CORRECTLY IMPLEMENTED for Archives — first full-chain confirmation.** Unlike EVIDENCE_prd14 (dashboard_widgets), EVIDENCE_prd14d (family_best_intentions), this addendum's host table chain works end-to-end in `_shared/context-assembler.ts:557-625`. **Upgrade recommendation:** change the §5.2 consolidation framing from "three-tier toggle drift pattern" to "three-tier toggle adopted inconsistently — Archives is the reference, other surfaces must reach parity." Archives is the pattern's proof point.

## Orchestrator adjudication

(empty — pending walk-through)
