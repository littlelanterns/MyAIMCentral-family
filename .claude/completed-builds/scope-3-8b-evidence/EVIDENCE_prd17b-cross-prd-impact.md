---
Status: COMPLETE (worker analysis captured by orchestrator under Option B report-only protocol)
Stage: C
Scope: 3 + 8b
Opened: 2026-04-20
Addendum: prds/addenda/PRD-17B-Cross-PRD-Impact-Addendum.md
Bridged PRDs: PRD-17B (source) ↔ PRD-01 (families columns + auto-provision), PRD-04 (QuickTasks Wand2), PRD-05 (mindsweep-sort + mindsweep-email-intake Edge Functions, "MindSweep this" trigger), PRD-08 (RoutingStrip destination + Notepad input), PRD-14B (calendar parsing + source_type='mindsweep'), PRD-15 (cross-member family_requests), PRD-17 (studio_queue columns + source CHECK enum), PRD-18 (Section #28 MindSweep Digest + MindSweep-Lite reuse), PRD-22 (Settings section), PRD-33 (PWA manifest, IndexedDB queue, Web Share Target API)
Provenance: Worker `a28574413665b3886` (Opus, report-only mode) ran the full evidence pass in-memory across the addendum + full PRD-17B + four Edge Functions (`mindsweep-sort`, `mindsweep-email-intake`, `mindsweep-auto-sweep`, `mindsweep-scan`) + `useMindSweep.ts` + migration `00000000100089_mindsweep_tables.sql` + `studio_queue` original definition + `commitMindSweepLite.ts` (PRD-18 Rhythms reuse path). Worker returned structured findings as completion text per Option B protocol; orchestrator persisted verbatim.
---

## Worker cover paragraph

Walked the PRD-17B integration surface end-to-end. Read the addendum + full PRD-17B + four Edge Functions (`mindsweep-sort`, `mindsweep-email-intake`, `mindsweep-auto-sweep`, `mindsweep-scan`) + `useMindSweep.ts` (the client routing layer that actually writes the rows the Edge Function only classifies) + migration `00000000100089_mindsweep_tables.sql` + `studio_queue` original definition (migration `…000008` line 442) + `commitMindSweepLite.ts` (the PRD-18 Rhythms reuse path). Identified 14 integration seams collapsing to 8 distinct findings. The traversal exposed two SCOPE-8b findings (auto-sweep semantically broken — pulls items from holding, classifies, then strands the results because `mindsweep-sort` returns classifications WITHOUT writing rows; and `classify_by_embedding` RPC has no `is_safe_harbor` / `is_included_in_ai` / `is_privacy_filtered` filtering despite running over context-source tables) and six SCOPE-3 findings spanning addendum schema-spec drift across multiple destinations. The most load-bearing surprise: the `mindsweep-sort` Edge Function only returns classification results — actual writes to `studio_queue` and 7+ destination tables happen in client-side `useMindSweep.routeSweepResults`. This means `mindsweep-auto-sweep` (server-side cron) calls `mindsweep-sort` from the server but never invokes a routing layer, so the holding items get classified, marked `processed_at=now()`, and then evaporate with zero rows created in any destination. The "auto-sweep at 8pm" experience promised in PRD-17B Screen 2 is a cosmetic stub. PRD-17B is also the LARGEST source-discipline-violation contributor in the cross-addendum pattern — addendum names FOUR new `studio_queue.source` enum values that don't exist as a CHECK constraint at all (column is freeform TEXT), reinforcing the pattern PRD-15 + PRD-23 evidence files already opened.

## Per-seam two-column table

| # | Seam | Addendum spec | Code reality | Classification | Proposed finding tag | Beta default |
|---|---|---|---|---|---|---|
| 1 | `families.sweep_email_address` + `sweep_email_enabled` columns auto-generated on family creation | Addendum L11–14: columns generated on family creation, dormant until enabled | Columns exist on `families` (live_schema.md L52, L54). NO auto-generation trigger found — `sweep_email_address` is NULL for all 3 live families. `useUpdateSweepEmail` only flips the boolean; address generation is unimplemented. | Unintentional-Fix-Code (partial-wiring) | **SCOPE-3** | N |
| 2 | `mindsweep_settings` auto-created for Mom/Dad/Independent on family setup; Mom + Dad emails seeded into `mindsweep_allowed_senders` | Addendum L13–14: auto-create alongside Backburner; auto-seed senders | `mindsweep_settings` rows = 0 in live_schema (despite 18 family_members and PRD-claimed auto-creation). `mindsweep_allowed_senders` = 0. No trigger or `auto_provision_member_resources` extension found in migrations. Defaults silently fall back via `useMindSweepSettings` `.maybeSingle()` returning null → JS-default aggressiveness. | Unintentional-Fix-Code (partial-wiring) | **SCOPE-3** | N |
| 3 | `studio_queue.source` CHECK adds 4 new values: `'mindsweep_auto'`, `'mindsweep_queued'`, `'email_forward'`, `'share_to_app'` | Addendum L94: "Update `source` CHECK constraint" | NO CHECK constraint exists on `studio_queue.source` — column is freeform `TEXT NOT NULL` (migration `…000008:449`). All 4 values write fine because there's no constraint to satisfy or violate. **Cross-addendum pattern:** PRD-15 evidence seam #9 + PRD-23 cross-pattern flagged the same root cause across other tables. PRD-17B is the LARGEST contributor — 4 of the 4 promised values are unenforced. | Unintentional-Fix-Code | **SCOPE-3** | N |
| 4 | `mindsweep-sort` Edge Function performs classification + routing | Addendum L43: Edge Function does "extraction + classification + confidence scoring + routing" | `mindsweep-sort/index.ts:99–383` ONLY classifies and returns results. Inserts ZERO rows into destination tables and ZERO rows into `studio_queue`. All actual routing happens in client-side `useMindSweep.ts:280–514` (`routeSweepResults` → `routeDirectly` / `queueForReview`). | Unintentional-Fix-PRD (architecture differs from spec but works for client-driven flows) | **SCOPE-3** | N |
| 5 | **Auto-sweep at member's configured `auto_sweep_time` processes the holding queue** | PRD-17B L757–759 + addendum L754: "Auto-sweep scheduled function runs daily at member's configured time" | `mindsweep-auto-sweep/index.ts:116` invokes `mindsweep-sort` from the SERVER, but `mindsweep-sort` only returns classifications — it does NOT write rows. The server has no `routeSweepResults` equivalent. Auto-sweep then marks holding items `processed_at=now()` (line 144–147) DESPITE no destination rows being created. Net effect: holding items disappear, no studio_queue / journal / victory / etc. rows materialize. **Auto-sweep is silently a no-op.** | Unintentional-Fix-Code (load-bearing wiring gap) | **SCOPE-8b** primary + SCOPE-3 cross-ref | **Y** (ADHD/disability-family use case is "I dump in the morning, see it sorted by 8pm" — silently dropping work breaks trust) |
| 6 | **`classify_by_embedding` RPC searches existing family content for semantic match (P2 cost optimization)** | PRD-17B L505: "Compare against existing family content embeddings in pgvector" | RPC exists at migration `…100089:234–327`. Searches `guiding_stars`, `best_intentions`, `self_knowledge`, `journal_entries`, `archive_context_items`. **NO `is_safe_harbor` filter** on `journal_entries` (Safe Harbor entries leak into classification matches per CLAUDE.md #243). **NO `is_included_in_ai` filter** on any table (HeartOff'd items still match — three-tier toggle bypassed). **NO `is_privacy_filtered` filter** on `archive_context_items` (privacy-filtered items leak per CLAUDE.md #76). **SECURITY DEFINER + no caller authorization check** — `p_family_id` is trusted from caller. | Unintentional-Fix-Code | **SCOPE-8b** primary + SCOPE-3 cross-ref | **Y** |
| 7 | Cross-member routing creates `family_requests` row with `source='mindsweep_auto'` | Addendum L82 + CLAUDE.md #145 | Main MindSweep path (`useMindSweep.queueForReview:482–514`) writes to `studio_queue` with `requester_id` set, NOT `family_requests`. The ONLY path writing `family_requests` with `source='mindsweep_auto'` is `commitMindSweepLite.ts:190–220` (PRD-18 Rhythms-Lite path, Build L.1, adult-only). The main MindSweep capture flow never satisfies the addendum's PRD-15 integration — it's a queue routing, not a request. | Unintentional-Fix-Code (partial-wiring) | **SCOPE-3** | N |
| 8 | Calendar items route through `calendar-parse-event` Edge Function | Addendum L66 + PRD-17B Outgoing Flows L463: "Directly to `calendar-parse-event` Edge Function (PRD-14B) for event extraction" | NO `calendar-parse-event` Edge Function exists in `supabase/functions/`. Calendar parsing happens INSIDE `mindsweep-sort` (the `normalizeCalendarDetail` helper at L775–894), output lands in `studio_queue` with `destination='calendar'` for review in CalendarTab. Works, but architecturally diverges from addendum. | Unintentional-Fix-PRD (addendum stale; consolidation in mindsweep-sort is the actual design) | **SCOPE-3** | N |
| 9 | Recipe destination dual-routes: recipe → archives, ingredients → list | Addendum + PRD-17B Outgoing Flows: dual-routing for recipes | `mindsweep-sort` returns metadata `{dual_route: true, recipe_to: 'archives', ingredients_to: 'list'}` (Edge Function L362). `useMindSweep.routeDirectly:351–365` collapses recipe to a SINGLE list queue insert. Archives side never written. Ingredients-vs-recipe split exists in metadata only; never materializes. | Unintentional-Fix-Code (partial-wiring) | **SCOPE-3** | N |
| 10 | `calendar_events.source_type` enum gets new value `'mindsweep'` | Addendum L74: "Add `'mindsweep'` to the `event_source` enum/CHECK values" | `calendar_events.source_type` is freeform TEXT (no CHECK in live_schema). Calendar items don't even land directly in `calendar_events` from MindSweep — they pass through studio_queue review. The enum addition is moot because the destination never receives MindSweep-source rows directly. | Unintentional-Fix-PRD (addendum spec not aligned with actual flow) | **SCOPE-3** | N |
| 11 | `mindsweep_holding.content_type` accepts every input shape MindSweep supports | PRD-17B Schema L341 lists 6 values | Migration CHECK constraint allows `'voice_short','voice_long','text','scan_extracted','link','email'` (migration `…100089:60–61`). Edge Function input schema and `useMindSweep` types both accept `'calendar_file'` (used for `.ics` import flow per useMindSweep:602). `useImportCalendarEvents` writes directly to `studio_queue`, BUT a paste of `.ics` content into the holding queue would fail the CHECK constraint. Source-channel CHECK is also missing `'auto_sweep'` and `'rhythm_evening'`. | Unintentional-Fix-Code (drift between schema CHECK and code) | **SCOPE-3** | N |
| 12 | `mindsweep_events.processing_cost_cents` populated per event for digest analytics | PRD-17B Schema L389 + Decisions L834 cost-context relies on cost tracking | `mindsweep-sort/index.ts:339` inserts `processing_cost_cents: 0` and never updates the row post-LLM. Comment "Updated after LLM cost is known" is aspirational. Cost data exists in `ai_usage_tracking` (via `logAICost`) but per-event totals never surface for digests. | Unintentional-Fix-Code (partial-wiring) | **SCOPE-3** | N |
| 13 | "MindSweep this" recognized trigger phrase in LiLa conversations routes content through `mindsweep-sort` | Addendum L40 | No grep hits for "MindSweep this" or `mindsweep_this` trigger detection in any LiLa Edge Function or `lila-chat` system prompt. Stub. | Unintentional-Fix-Code (absent integration) | **SCOPE-3** | N |
| 14 | Section #28 "MindSweep Digest" added to PRD-18 Section Type Library, default-on for Morning/Evening/Weekly rhythms | Addendum L107–113 | `mindsweep_settings.digest_morning/digest_evening/digest_weekly` columns exist with defaults `true`. NO MindSweep Digest section type seeded in `rhythm_configs` or rendered in any rhythm section component (grep for "mindsweep_digest" / "MindSweep Digest" returns nothing). The toggles persist user preference for a feature that doesn't render. | Unintentional-Fix-Code (absent integration) | **SCOPE-3** | N |

## Unexpected findings list (seams not covered in addendum)

1. **Mom-only `mindsweep_settings` defaults silently apply** — `useMindSweepSettings` returns null when no row exists, JS falls back to `'always_ask'` aggressiveness. Without `auto_provision` seed (Seam #2), every member operates as if "Always Ask" without explicit consent.
2. **SCOPE-8a.F7 already adjudicated cross-addendum** — `routeDirectly` writes `source: 'manual'` to `victories`, `guiding_stars`, `best_intentions`, `self_knowledge`, erasing the audit trail of MindSweep-routed content. Already a Medium SCOPE-8a finding — confirmed in code, do not re-emit.
3. **Calendar attendee detection is impressively wired** — `mindsweep-sort` `detectCalendarAttendees` + `resolveAttendeeNameList` (lines 929–1065) do union-of-regex-and-LLM detection with explicit driver parsing. Better than addendum specified. Worth documenting as positive.
4. **Teen Crisis Override exemption is ROUTE-LEVEL, not wired** — RECONNAISSANCE_REPORT_v1 L489 ruled `mindsweep-sort` Crisis Override absence "Intentional, not a finding" because mom's brain dump is private. But `/sweep` is `ProtectedRouteNoShell` (App.tsx:107) — accessible to any authenticated user including Independent teens. PRD-17B says teens get capture access. Crisis Override exemption rationale ("mom's brain dump") breaks at the teen capture surface. RECON ruling explicitly noted: "Flag if teens/children get entry points." → They do.

## Proposed consolidation

### §5.1 within-addendum consolidation
- Seams #5 (auto-sweep no-op) and #6 (`classify_by_embedding` filter bypass) are the two SCOPE-8b primaries. Different root causes — keep separate.
- Seams #1, #2, #11, #12, #13, #14 are all "addendum claims wired, code is partial/absent." Different surfaces, different remediation paths — keep as 6 individual SCOPE-3 findings.
- Seams #3 + #10 + (cross-pattern Seam #11 schema CHECK) all touch `enum CHECK constraints addendum demanded but don't exist`. Within PRD-17B: 3 distinct CHECK gaps. Consolidate into ONE PRD-17B SCOPE-3 finding "Multiple CHECK constraints addendum demanded never created or maintained" with sub-bullets. Then escalate the overall pattern to §5.2 cross-addendum.

### §5.2 cross-addendum candidates flagged for orchestrator review
**`source` CHECK constraint absence — CONFIRMED CROSS-ADDENDUM PATTERN.**

Worker recommends elevating to ONE consolidated SCOPE-3 cross-addendum finding. Contributing surfaces verified or strongly suspected:

| Surface | Evidence file | Confirmation |
|---|---|---|
| PRD-15 (`family_requests.source`) | `EVIDENCE_prd15-cross-prd-impact.md` seam #9 | Confirmed — PRD-28 migration `…100137` widened enum without amending PRD-15 addendum |
| PRD-23 (`tasks.source`, `guiding_stars.source`, `best_intentions.source`) | `EVIDENCE_prd23-cross-prd-impact.md` cross-pattern note | Confirmed — all freeform TEXT, no CHECK constraint |
| **PRD-17B (`studio_queue.source`)** | this file, seam #3 | **Confirmed — addendum demanded 4 new enum values for a CHECK constraint that never existed** |
| PRD-17B (`calendar_events.source_type`) | this file, seam #10 | Confirmed — addendum demanded `'mindsweep'` value for a CHECK that doesn't exist |
| PRD-08 (Notepad routing destinations) | `EVIDENCE_prd08-cross-prd-impact.md` cross-pattern + Findings F-A and F-B | Confirmed — `studio_queue.destination` is freeform TEXT; `useNotepad` writes `source: 'manual'` losing all traceability |
| PRD-16 (`meeting_agenda_items.source`) | not yet traversed | Suspected per addendum convention pattern |
| PRD-34 (ThoughtSift `lila_conversations.guided_mode`) | not yet traversed | Suspected — `personal_custom` etc. addendum-defined values not enforced |

Threshold for §5.2 escalation (3+ surfaces) is exceeded. Recommend a single consolidated `SCOPE-3.F{N}` finding titled "Source-attribution enum drift: addenda repeatedly demand CHECK constraint additions for constraints that don't exist; `source` columns across 6+ tables are freeform TEXT with no enforcement." PRD-17B is the heaviest contributor (2 promised CHECKs absent + 4 unenforced enum values).

## Proposed finding split

- SCOPE-8b primary (with SCOPE-3 cross-refs): **2** (seams #5 auto-sweep no-op, #6 embedding RPC filter bypass)
- SCOPE-3 only: **6** (seams #1, #2, #4, #7, #9, #13, #14 — collapsing #4 into "architecture-vs-spec divergence" if orchestrator prefers)
- Cross-addendum elevation: **1** (Seam #3 + #10 + sibling addenda — feeds into the consolidated cross-addendum source-discipline finding)
- Documented (no finding): **1** (Seam #8 — calendar-parse-event consolidation is a documented architectural choice; non-defective)
- Provisional: **0**

After consolidation: **2 SCOPE-8b + 4 SCOPE-3** (within-PRD-17B) + 1 contribution to cross-addendum = **6 PRD-17B-attributed findings + 1 cross-addendum contributor**.

## Beta Y candidates

2 (both SCOPE-8b primaries):
- **Seam #5: Auto-sweep silently a no-op.** Mom configures 8pm sweep; items disappear from holding without ever reaching destinations. ADHD/disability-mom use case ("dump in the morning, see it sorted by evening") is the marquee scenario in PRD-17B Screen 2 — non-viable without a fix.
- **Seam #6: `classify_by_embedding` bypasses Safe Harbor + is_included_in_ai + is_privacy_filtered + caller authorization.** Safe Harbor journal entries can surface as semantic matches; HeartOff'd items leak; privacy-filtered items leak; any authenticated user with a known family_id can query.

## Top 3 surprises

1. **`mindsweep-auto-sweep` is functionally a no-op.** It pulls items from `mindsweep_holding`, calls `mindsweep-sort` from the SERVER, marks items processed, and returns success. But `mindsweep-sort` only **classifies** — actual writes to `studio_queue` and 7+ destination tables happen in client-side `useMindSweep.routeSweepResults`. There is no server-side equivalent. Holding items vanish without producing destination rows. The "wake up to sorted items in your Queue" promise of PRD-17B is unimplemented despite the cron Edge Function existing.
2. **Recipe destination claims dual-routing in metadata, performs single-routing in code.** The Edge Function returns `{dual_route: true, recipe_to: 'archives', ingredients_to: 'list'}` but `useMindSweep.routeDirectly:356` collapses recipe → list with no archive insert. Frontend never reads the dual-route metadata.
3. **`classify_by_embedding` RPC has zero privacy filtering and zero caller authorization.** The RPC is `SECURITY DEFINER`, accepts any `p_family_id`, and runs SELECTs against `journal_entries` (no `is_safe_harbor` filter), `archive_context_items` (no `is_privacy_filtered` filter), and ALL context-source tables (no `is_included_in_ai` filter at any tier). A staff RLS bypass plus this RPC = a path to read every family's Safe Harbor journal embeddings via similarity matching.

## Watch-flag hits

- **F11 server-side enforcement** — Seam #6 is the most direct hit. `classify_by_embedding` is server-defined but has NO authorization check; any authenticated user can pass any `p_family_id`. Confirmed.
- **Crisis Override** — `mindsweep-sort` exempt per RECON ruling (mom's private brain dump). BUT teens have route-level access (`/sweep` is `ProtectedRouteNoShell`). Per the RECON conditional ("Flag if teens/children get entry points"), the exemption no longer holds. Worker recommends orchestrator escalate to a Provisional SCOPE-8b for teen-surface Crisis Override coverage in `mindsweep-sort`.
- **F17 messaging** — Seam #7 reinforces the Notepad → Message orphan pattern from PRD-15 evidence file. Cross-member MindSweep does NOT write to `family_requests` table from main path; only PRD-18 Rhythms-Lite path satisfies the convention.
- **F22 + F23 (privacy)** — Seam #6 partially intersects: HeartOff'd context-source items leak into MindSweep classification matches. Violation of "checked somewhere, checked everywhere" CLAUDE.md #75 principle.
- **studio_queue source discipline** — Strongest contributor. PRD-17B is the LARGEST source-discipline-violation surface — 4 promised enum values, 0 enforced. Must consolidate cross-addendum.
- **`is_included_in_ai`** — Seam #6 confirms three-tier toggle bypass at the embedding-similarity layer.

## Orchestrator adjudication

(empty — pending walk-through)
