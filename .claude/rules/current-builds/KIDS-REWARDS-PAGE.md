# Active Build: KIDS-REWARDS-PAGE — Kids' My Rewards Page + Reward Unification

> **Status: BUILD ACTIVE — SLICE 1 COMPLETE (committed `e4f41d0`, founder eyes-on cleared, pushed 2026-06-12). SLICE 2 CODE COMPLETE (2026-06-12, Worker KIDS-REWARDS-PAGE-S2) — slice2 suite 8/8 + slice1 pin 15/15 + regression pins 44/44 + eyes-on tour 8/8 green; ZERO new migrations (the 100266 visibility model carried Slice 2); NOTHING COMMITTED — awaiting founder eyes-on per gate. See "Slice 2 Progress Log" below. Next free migration: 00000000100273.** Slice plan + rulings founder-approved 2026-06-12 (R1/R2/R3/R5/R6 as ruled; R4 REVISED: no mom sidebar entry — mom's personal rewards = her own pill in PrizeBoard Prizes tab, self-only render, excluded from owed strip/by-date list). Adult self-rewards: any adult self-proposes; dad rides /my-rewards; per-reward visibility private-default/shareable via member picker, enforced at query layer (never rendered-then-hidden). NEW VISIBILITY CONVENTION: mom-sees-all default incl. adults' private self-rewards; mom may grant per-member "personal rewards privacy" (query-layer, reversible, lives in Gamification Settings My Rewards section). Slice 1 addition: reward image picker is THREE-MODE everywhere (text-only default / upload / platform library) with embedding auto-suggest (platform_assets fully embedded + match_assets() live + TaskIconPicker precedent — no new pipeline). R1+R2 follow-ups registered in STUB_REGISTRY 2026-06-12.
> Worker/orchestrator: KIDS-REWARDS-PAGE · Gate resolved 2026-06-12
> Authority chain: `claude/feature-decisions/KIDS-REWARDS-PAGE-Gate-Decisions.md` (wins) → `claude/feature-decisions/Kids-Rewards-Page.md` (recon)
> All gate scope ships in this build. No MVP shortcuts. Playwright is the only proof of done.

---

## Section 8 Live-Recon Findings (completed 2026-06-12 — never guessed, all cited)

1. **Routine rewards (Q5d):** Per-step reward config does NOT exist anywhere (`task_template_steps` has zero reward columns; zero code paths). Routine rewards live at template level (`task_templates.default_reward_type/amount`) and pay out through the SAME task-completion paths as tasks: completion-time `fireDeed` at `useTaskCompletion.ts:169` (+ app-side money grant :187–205 reading `task_rewards`) and approval-time at `useTaskCompletions.ts:181`. → Routine-COMPLETION rewards wire now at those two points; per-step rewards are a NEW FEATURE, not a hook → **registered follow-up per guardrail G2** (flagged for founder in slice plan).
2. **Trackers (G1):** 8 tracker types are real (config + data recording): streak, grid, tally, timer_duration, collection, percentage, sequential_path, dot_circle (+ color_reveal, gameboard, randomizer_spinner etc. in the type union). BUT **no tracker has goal-reached/completion detection that fires anything** — `widget_data_points` record silently. Prize config today = text-only `prize_label` in `widget_config` (WidgetConfiguration.tsx:662). `reward_reveal_attachments.source_type` already permits `'widget'` but nothing fires it. → Per G1/G2: add `prize_image_url` to tracker prize config UI now (image option); earned-prize firing on tracker completion is **registered covered-by-principle** until a build adds real goal detection (flagged for founder).
3. **`member_sticker_book_state.active_page_id`:** nullable; written at page-unlock (set to newest unlocked) and used as the award landing page; **NOT updated on manual page switch** (modal switch is local state only). → "Last-viewed" needs **one new column `last_viewed_page_id`** (founder pre-authorized) so award-landing semantics stay untouched.
4. **Unplaced creatures:** `sticker_page_id` IS nullable but never null today (award always lands on active page with random 0.05–0.90 positions). `useMoveCreature` ALREADY accepts `newPageId`. No unplaced catalogue UI, no remove-from-page path. → Build: catalogue view (all earned incl. unplaced), remove-from-page (set null), place-from-catalogue, cross-page drag.
5. **Widget grid:** REAL on Guided (`GuidedWidgetGrid` → `DashboardGrid`, reorder-only), Independent/Adult (`Dashboard.tsx` full edit), with S/M/L sizes. Play is hardcoded (PlayStickerBookWidget + ColorRevealWidget inline, not dashboard_widgets rows). Registration path for new types: `types/widgets.ts` union → `WidgetRenderer.tsx` case → `widget_starter_configs` seed row → picker category. → Register `'sticker_page'` + `'coloring_page'` as real catalog widgets; Play keeps its hardcoded equivalents.
6. **family_requests:** Solid lifecycle (create/accept/decline/snooze/discuss, RequestsTab, notifications) but **NO JSONB payload column** and status model is mom-directional (no "ball returns to kid" state for a counteroffer). → Per founder's verify-then-choose: **dedicated `reward_proposals` table** (terms JSONB, counter_terms, statuses incl. countered + kid accept/decline), with proposal cards RENDERED INSIDE the existing Queue RequestsTab so mom's one-decision-inbox principle (Convention #66) holds.
7. **Member-agnosticism:** Pipeline is fully role-blind end to end ✓ (dispatch matches family_member_id not role; both godmothers extract member from the deed; `fireDeed` sites have no role filters; ContractRevealWatcher mounted in ALL five shells incl. MomShell). TWO gaps: (a) `earned_prizes` UPDATE RLS = mom/additional_adult only → kid redeem needs a **`redeem_own_prize` SECURITY DEFINER RPC** (own row, redeem fields only); (b) **mom's sidebar is FROZEN (Convention #274) and has no My Rewards entry** → mom's own page requires a frozen-sidebar amendment (flagged for founder).
8. **Victories:** `victories` (13 rows: description, photo_url, life_area, timestamps) renders fine; full-celebration narrative lives in `victory_celebrations` (0 prod rows; written on child approval in DailyCelebration via saveCelebration). `victories.celebration_text` is a DEAD column — never populated; do not use. No existing full-celebration browse modal. Join is array-membership (`victory.id = ANY(victory_celebrations.victory_ids)`). → Build `CelebrationDetailModal`; victories list → tap → narrative when one exists, else victory description + photo.
9. **Provenance values (final):** `'task_completion'` (completion + approval sites), `'intention_iteration'`, `'randomizer_item'` (approval), `'task'` (practice log), `'sequential_task'` (mastery), `'contract_grant'` (fallback in godmothers). History resolver joins against these.

---

## Slice Plan (dependency-ordered — pending founder approval)

**Slice 1 — Reward pipeline core (Unification Principle backbone).**
Migration 100266+: `reward_image_url` on `tasks`, `task_templates`, `list_items`, `custom_reward_godmother_configs`; `execute_custom_reward_godmother` snapshots image+name into `earned_prizes` (fixes 100214 text-only gap); `redeem_own_prize` RPC; tracker prize-image field (`prize_image_url` in widget_config + WidgetConfiguration upload). App: privileges/family_activities → `earned_prizes` hook at `useTaskCompletion` (no-approval items, completion-time) + `useTaskCompletions` (approval-required items, approval-time — Q7 timing rule), covering tasks/routines/opportunities via the shared path; provenance populated. PrizeBox: render `prize_text` (bug fix), kid Redeem w/ confirm + quiet mom notification, Play "Ask a grown-up to use it!". PrizeBoard: mom Un-redeem (clears redeemed_at/by), edit-image-later. RewardConfig + opportunity editors + ContractForm: optional image upload (privileges/family_activities), `ImageUploader` → `gamification-assets/reward-images/{familyId}/`.

**Slice 2 — My Rewards page + opt-in + Points/Finances/Custom/Victories sections.**
GamificationSettingsModal "My Rewards Page" section (`show_my_rewards` + `family_members.preferences.my_rewards_sections`; finances default mirrors `child_can_see_finances`; propose + victories default OFF). Shared `MyRewards` component at `/my-rewards` (Guided/Independent/Adult) and Play `/rewards`: Points section (per-child currency + streak), Finances section (ONE owed number = reconciled with Balance page, Convention #271 test; click-through to `LedgerView mode='self'`; never on Play), Custom Rewards section (active cards + Previously Redeemed click-in history w/ provenance resolver), Victories section (list + `CelebrationDetailModal`). Mom's own page: sidebar entry (frozen-sidebar amendment), same component, her prizes NEVER on PrizeBoard.

**Slice 3 — Creature pages + Coloring section + Dashboard Doors.**
Migration: `member_sticker_book_state.last_viewed_page_id` + widget seed rows. Creature section (**design REFINED 2026-06-12 — see Gate-Decisions §13, founder direction during Slice 2 close-out**): **horizontal swipe strip of backgrounds for NAVIGATION** (swipe side-to-side through unlocked `gamification_sticker_pages` for the active `theme_id`, each rendering its placed creatures) + **drag-to-edge page flip RETAINED for MOVING a creature between backgrounds** (founder: "we still want drag to edge page flip... moving them from page to page is still desired" — phone-home-screen metaphor: swipe browses, drag-to-edge carries) + **self-contained FRAME holding the swipe strip (hero) + an unplaced-creatures scrollbar at the BOTTOM OF THE FRAME** (`member_creature_collection` where `sticker_page_id IS NULL`; bottom-of-frame NOT bottom-of-page → no Play-nav collision) that is **hideable via the shared `<PullTab orientation="bottom">`** (`src/components/shared/PullTab.tsx`, Notepad-drawer pattern) so a finished scene becomes the hero + **dual placement gesture** (touch-select-then-touch-place AND drag-and-drop) via `useMoveCreature`; remove-from-page = `sticker_page_id=NULL`. **Prepare-for-now (build later):** background/character-SET picker over `gamification_themes` (1 row today) writing `active_theme_id` — section must read the active set + filter by `theme_id` from the start, never hardcode the single theme. `last_viewed_page_id` restores swipe position. Reuse `StickerOverlay` drag + 0–1 coords + `useMoveCreature newPageId`. Play small-finger eyes-on required (esp. the drag-to-edge flip threshold — reachable without accidental flips when placing near an edge). Coloring section: active + completed reveals, keep print flow, ADD Download (lineart PNG + finished color PNG). New widgets `'sticker_page'` (last-viewed page miniature, view-only viewport → modal) + `'coloring_page'` (active reveal miniature → modal): types union + WidgetRenderer cases + seeds + picker.

**Slice 4 — Propose-a-Reward + mom self-propose.**
Migration: `reward_proposals` table (proposer/family/terms JSONB {want_text, want_image_url, earn_structure: once|streak_n_days|finish_list, params}, status pending/countered/accepted/declined, counter_terms, created-artifact refs, RLS). Kid guided form in the `propose` section (Guided+). Mom processing cards inside Queue RequestsTab: Approve → prefill-confirm into existing creation flow (task/tracker/routine) with reward_type=privileges + image per Q5; Counter (edit prefill, one round, kid accepts/declines); Decline. Mom self-propose = collapsed single screen on her page. Earned outcomes flow through Slice 1 pipeline.

**Slice 5 — Parent PrizeBoard + full verification.**
Prizes tab "By kid / By date" arrangement toggle (LedgerView localStorage pattern) + prizes/privileges-only summary strip (NO allowance dollars). Playwright `tests/e2e/features/kids-rewards-page.spec.ts` (+ proposal spec), tsc -b, per-slice lint closure, mobile parity, Mom-UI table, LiLa knowledge (help-patterns + feature-guide-knowledge), FeatureGuide, #271 owed-number reconciliation test, regression pins (leak-pass, permissions-wiring) stay green.

**Dispatch order:** 1 → 2 → 3 ∥ 4 → 5. Migrations via `supabase db push --linked`; selective staging; founder eyes-on before push.

## Rulings flagged for founder confirmation (within slice-plan approval)

R1. Per-step routine rewards don't exist (no schema, no code) — completion-level wired now; per-step = registered follow-up per G2.
R2. Trackers have NO completion detection — image field added to tracker prize config now; earned-prize firing = registered covered-by-principle per G1/G2.
R3. Proposals get a dedicated `reward_proposals` table (family_requests has no payload column + can't carry a counter round); proposal cards render inside the existing Queue RequestsTab so the one-inbox rule holds.
R4. Mom's FROZEN sidebar (Convention #274) gains one "My Rewards" entry — explicit amendment.
R5. `last_viewed_page_id` = new column; `active_page_id` award-landing semantics untouched.
R6. Kid redeem via `redeem_own_prize` SECURITY DEFINER RPC (RLS UPDATE is parent-only today; RPC limits kids to redeem fields on own rows).

## Slice 1 Progress Log (2026-06-12)

**Migrations applied to linked production (in order):**
- `100266_kids_rewards_pipeline_core.sql` — reward_description/reward_image_url/reward_image_asset_key on tasks + list_items (+ default_* on task_templates) + custom_reward_godmother_configs image cols; earned_prizes visibility model (visibility/shared_with_member_ids/created_by/awarded_completion_id + UNIQUE); RLS rewrite (visibility-aware SELECT/UPDATE + util.personal_rewards_privacy SECURITY DEFINER helper); `redeem_own_prize` RPC (earner-only + quiet mom notification, category 'gamification'); `award_custom_reward_for_completion` RPC.
- `100267_award_custom_reward_live_vocabulary.sql` — **ground-truth correction:** live reward vocabulary is `'privilege'`/`'custom'` (TaskCreationModal Section 7 inline UI; RewardConfig.tsx was type-only dead vocabulary). RPC filter widened to ('privilege','custom','privileges','family_activities').
- `100268_custom_reward_godmother_round3.sql` — **caught + fixed a self-inflicted regression:** 100266's godmother rewrite was based on superseded 100214, dropping round-2's NULL-config guard (100223) → would crash every inline-payload contract. Round 3 = 100223 body + image snapshot chain (per-item list → config table → contracts.payload_config) + prize_name + visibility/created_by.

**App code (all tsc -b clean, lint 0 errors):**
- `src/lib/connector/awardCustomReward.ts` — never-throws RPC wrapper (grantMoney pattern).
- Hook sites wired (4): `hooks/useTasks.ts` useCompleteTask (!requireApproval) + useApproveTaskCompletion; `hooks/useTaskCompletions.ts` useApproveCompletion; `components/tasks/useTaskCompletion.ts`. Q7 timing enforced by the RPC.
- `src/components/rewards/RewardImagePicker.tsx` — three-mode picker (none/upload/library) with embedding auto-suggest via useTaskIconSuggestions (tag instant + match_assets refine).
- TaskCreationModal Section 7: privilege/custom → "What's the reward?" + picker; amount hidden for custom types. defaultReward + RoutineDeployModal initializers extended; edit hydration in BOTH re-init blocks + TaskEditModal + DashboardTasksSection (prevents edit-save nulling — checkbox-honesty class); `useTaskEditor.saveEditTask` writes the three columns; `createTaskFromData` writes task columns + task_rewards row (custom types persist WITHOUT amount; description into reward_value).
- `RewardConfig.tsx`: RewardType union widened to live vocabulary; CUSTOM_REWARD_TYPES exported; description+picker added (component remains unmounted/type-source).
- `useOpportunityLists` claim path: bridge task snapshots reward_description/image from list item; task_rewards row persists for custom types without amount. `types/lists.ts` ListItem + `types/tasks.ts` Task extended.
- PrizeBox: prize_text fallback render (bug fix), `selfRedeem` mode w/ "Use your reward now?" confirm → `useSelfRedeemPrize` (RPC), `playMode` → "Ask a grown-up to use it!"; PlayRewards passes playMode.
- PrizeBoard Prizes tab: Recently redeemed (30-day) group w/ mom Un-redeem (`useUnredeemPrize`), edit-image-later modal (`useUpdatePrizeImage`, prize_type guarded for randomizer/celebration types), PrizeThumb honors platform_image.
- ContractForm: custom_reward gets the picker; image rides `contracts.payload_config` (dispatcher passes it through; round-3 godmother reads it).
- WidgetConfiguration sequential_path: prize picture fields (`widget_config.prize_image_url/prize_image_asset_key`) next to prize_label (G1/R2 — firing is the registered follow-up).

**Registered:** R1/R2 + 4 post-MVP follow-ups in STUB_REGISTRY ("KIDS-REWARDS-PAGE Follow-Ups").

**Slice 1 verification COMPLETE (2026-06-12, Worker KIDS-REWARDS-PAGE-S1V):**
`tests/e2e/features/kids-rewards-slice1.spec.ts` written and GREEN — 10/10 covering all six baton-pass items. **The NULL-guard pin did its job on first run:** migration 100268's round-3 rewrite had reintroduced Bug C3 (the unassigned `v_list_item` RECORD reference originally fixed by 100224) — every inline-payload custom_reward contract was failing in production with `record "v_list_item" is not assigned yet` (contract_grant_log status='failed', error swallowed into grant-log metadata by the dispatcher's exception handler). Fixed by `00000000100269_custom_reward_godmother_round4.sql` (applied to linked production 2026-06-12): restores the C3 scalar fix while preserving the round-2 NULL-config guard and the round-3 image chain. **Next free migration number is 100270.** Test 1 now pins BOTH regressions (NULL config + text-mode unassigned record) plus the payload_config image snapshot. **Guard ledger added (founder follow-up, same session):** every known guard in `execute_custom_reward_godmother` now has its OWN named pin — G1 NULL-config [1a], G2 Bug-C3 scalar fix [1a+1c], G3 NULL-member graceful fail [1f], G4 list_reference-without-list graceful fail [1e], G5 payload_config image [1b], G6 config image [1c], G7 per-item list image wins + item consumed [1d], G8 empty-payload fallback text [1g]. The ledger itself lives as a header comment in migration 100269 ("base any rewrite on THIS file") — the regression-by-copying-superseded-bodies failure mode now trips over its own history. Suite is **15/15 green**; zero fixture residue verified in production; tsc -b clean, lint 0 errors; regression pins (leak-pass 10, permissions-wiring 14, task-assignment-scoping 8, fo-command-center 12 = 44/44) re-run same session. Founder ruling: NO backfill of historical failed grants (family-only data, rules new). NOTHING COMMITTED — awaiting founder eyes-on per the gate below (incl. the empty-suggestion state on the image picker with a non-matching word).

**Eyes-on tour + second pre-existing bug find (2026-06-12, same session):** founder requested a headed walkthrough of the 5 mom surfaces → `tests/e2e/features/kids-rewards-eyes-on-tour.spec.ts` (gated behind `EYES_ON_TOUR=1`, never runs in normal suites; screenshots to gitignored `eyes-on-tour/` at repo root; demo fixtures prefixed `KRSLICE1 EYES-ON` are left in place for live poking and swept by the slice-1 suite's next run). **The tour caught a real pre-existing bug:** `WidgetConfiguration`'s trackerType/title/config are one-shot `useState` initializers, and Dashboard.tsx + Studio.tsx mount the modal PERMANENTLY with `starterConfig=null` — so every widget Customize on those two pages opened frozen as "Tally Counter" (FamilyOverview's conditional render was never affected, which is why FO tracker-create verified fine in the FO build). Fixed with a remount `key={selectedStarterConfig?.id ?? 'widget-config-idle'}` at both call sites (Dashboard.tsx + Studio.tsx now in the build's modified set). Also noted, NOT fixed (display quirk, register as follow-up): seeded `widget_starter_configs.category` values ("Special Needs & Therapy" etc.) don't match WidgetPicker's internal category keys (`goal_pursuit` etc.), so the three seeded sequential_path starter configs never render in the picker — only the registry "Step Path" entry shows. Tour 5/5 green after fixes; slice-1 suite + all 4 regression pins re-run after the app-code change (58 passed + 1 unrelated FO-Finances flake-then-pass). **Suggestion-pipeline false alarm resolved:** early tour screenshots showed zero picker suggestions for "a popsicle"/"an ice cream trip" — root cause was TEST INFRASTRUCTURE, not the app: the slice-1 spec's `signOut()` defaulted to GLOBAL scope, revoking the tour's cached `.auth/*.json` sessions; REST kept working (stateless JWT) but `auth.getUser()`-validating Edge Functions (generate-query-embedding) 401'd, so Stage 2 silently fell back to nothing. Fixed: `signOut({ scope: 'local' })` in the spec + fresh-cache rerun proves the pipeline live (200 + Ice Cream 0.57 / Zoo Trip / Park Outing matches; final screenshots show the suggestion grids). Note for picker product expectations: suggestions draw from `category='visual_schedule'` assets only (411 active; Build M hook reuse per gate §11).

**Founder addition — picker library widened (2026-06-12, same session):** founder directive: reward/task image pickers must also surface tool icons (app_icon) and sign-in pictures (login_avatar) — "pizza"/"unicorn" live there with the "reward" tag. Changes: (a) migration `00000000100270` — `match_assets` gains `filter_categories TEXT[]` (legacy `filter_category` kept; array wins); (b) **migration `00000000100271`** — **dropped the platform_assets IVFFlat embedding index**: `ivfflat.probes` defaults to 1 of 100 clusters, so ANN scans returned EMPTY/partial results at random (this silently degraded Build M icon suggestions since 100025; function-scoped `SET ivfflat.probes` is permission-denied on Supabase hosted — Convention #246 GUC class). 622 rows = exact scan is microseconds; re-add tuned HNSW only if the library grows ~100×. **Next free migration: 100272.** (c) `useTaskIconSuggestions` gains `assetCategories` param (`REWARD_ASSET_CATEGORIES = visual_schedule + login_avatar + app_icon`); RewardImagePicker, TaskIconPicker, TaskCreationModal auto-suggest, and useTaskIconBrowseSearch all widened; vault_thumbnail deliberately excluded. (d) NEW `src/components/shared/PlatformAssetImage.tsx` — category/vibe-agnostic asset renderer replacing `FeatureIcon` at the 4 prize-render sites (PrizeBoard PrizeThumb, PrizeBox, RewardRevealModal, RewardImagePicker preview): FeatureIcon is app_icon-only AND classic-vibe-only, so platform_image prizes previously fell back to the Gift icon in every other theme — existing display gap closed. (e) `usePlayTaskIcons` explicit-key lookup category filter removed so picked avatar/tool icons render on Play tiles. (f) Embedding coverage now 622/622 — the 3 `best_intentions` app_icon rows had NULL display_name + NULL embedding (skipped by the original asset pipeline; platform_assets is NOT in the embed cron's TABLE_CONFIG); named/described/tagged in sibling style + embedded via generate-query-embedding, written by service role. (g) Incident logged: a PowerShell bulk import-swap mojibake'd non-ASCII chars in 4 files (PS 5.1 ANSI read of BOM-less UTF-8); caught by the tour's empty-state assertion, reversed cleanly (cp1252→UTF-8 transform), zero residue verified. Verified end-to-end: pizza→Pizza, unicorn→Unicorn/Dragon/Lion, a popsicle→Ice Cream ×4, brush teeth→teeth sequence (Build M intact). Tour re-run 5/5; slice-1 + 4 regression pins re-run after changes.

**Founder live eyes-on feedback batch (2026-06-12, same session):** six items from the founder exercising her real dashboard:
1. **Longer icon list + View more** — `useTaskIconSuggestions` fetch raised to 24 (threshold 0.3→0.2 so the closest images always surface); TaskIconPicker shows 8 inline + "View more (N)" expander, semantic-match order preserved (match_assets distance order). Emoji "✨" in the smart-match chip replaced with Lucide Sparkles (emoji rule).
2. **Browse All titles hidden** — TaskIconBrowser captions removed ("I don't want their version limited by our names"); names remain aria-label-only; images enlarged 64→72px.
3. **Weak-match honesty + miss ledger** — migration `00000000100272` creates `asset_suggestion_misses` (family-scoped RLS); **next free migration: 100273**. `GOOD_MATCH_SIMILARITY = 0.45`: searches settling below it (or with zero results) show "We don't have that exact image yet (we've made a note to add one) — here's the closest we have:" and log the term via `useAssetMissLogger` (session-deduped, fire-and-forget). Wired in TaskIconPicker ('task_icon'), RewardImagePicker ('reward_image'), TaskIconBrowser ('icon_browse'). The founder reviews the table to drive the asset pipeline backlog.
4. **Widget remove** — no change; founder found the edit-mode control.
5. **Wiggle gentled** — keyframes ±1.5°→±0.6°, WidgetCard duration 0.3s→1.6s (was ~3 sways/sec).
6. **Widget reorder persistence FIXED (real bug)** — first drag in auto layout-mode stashed the reorder behind a confirm banner rendered at the TOP of the grid (off-screen when dragging below the fold) and returned early — the drag silently snapped back; production sort_orders were all 0 (it had NEVER persisted). Per the component's own design note ("switches to manual on first user move"), the confirm gate is removed: reorder applies immediately + layout_mode flips to manual silently. DB write path verified end-to-end (RLS + update + refetch order).
Tour re-run 5/5 (zzzblorp stop now pins the weak-match message); slice-1 + 4 regression pins re-run after the batch.

**Original baton-pass scope (covered by the spec above):**
Playwright spec `tests/e2e/features/kids-rewards-slice1.spec.ts` covering, per founder directive at baton-pass approval:
1. **NULL-guard pin (REQUIRED):** an inline-payload custom_reward contract (godmother_config_id = NULL — what ContractForm authors today) must grant successfully end-to-end. This pins the round-2 hotfix restored by migration 100268 so it can never silently regress again.
2. **Both live vocabulary values (REQUIRED):** drive the popsicle pipe with reward_type `'privilege'` AND `'custom'` (the values migration 100267 accepts) — not just the PRD terms.
3. Award timing: require_approval task awards at APPROVAL (not at kid completion-mark); no-approval task awards instantly. Idempotency: re-firing the RPC on the same completion returns already_awarded, one earned_prizes row.
4. Kid self-redeem: `redeem_own_prize` RPC succeeds for the earner, sets redeemed_at/redeemed_by, fires the mom notification row; a kid's direct UPDATE on earned_prizes is RLS-blocked (parent-only policy probe).
5. Un-redeem round trip: mom clears redeemed_at/redeemed_by → card active again → kid can redeem again.
6. Visibility probes: a `visibility='private'` self-reward row NEVER reaches another (non-mom) member's client (SELECT returns zero rows under their session); `shared` row reaches only listed members; mom sees private rows UNLESS `family_members.preferences.personal_rewards_privacy=true` on the earner (probe both states).
Fixture hygiene: LEAKPASS-style prefixed fixtures created/removed via service role (role-scoping-leak-pass.spec.ts is the pattern reference); Testworth family.
**Gate:** founder does eyes-on of the 5 touched mom surfaces in parallel (TaskCreationModal Section 7, PrizeBoard Prizes tab, ContractForm custom reward, WidgetConfiguration sequential_path, PlayRewards/PrizeBox). NOTHING COMMITS until BOTH the suite passes AND eyes-on clears. Then selective staging (only KIDS-REWARDS-PAGE files), founder confirms before push.

## Slice 2 Progress Log (2026-06-12, Worker KIDS-REWARDS-PAGE-S2)

**Scope shipped (per the approved Slice 2 paragraph, R4-REVISED applied — NO mom sidebar entry, Convention #274 untouched; mom's PrizeBoard pill lands with Slice 5's Prizes-tab restructure, self-propose with Slice 4):**

**Zero migrations.** The 100266 visibility model + RPCs carried all of Slice 2.

**New files:**
- `src/hooks/useMyRewardsSettings.ts` — read/write for `preferences.show_my_rewards` + `my_rewards_sections` (explicit overrides only; defaults resolve at READ time per Q1) + `personal_rewards_privacy`. Q1-approved change implemented: finances default MIRRORS the primary pool's `child_can_see_finances`; PRD-28 hard rule forces finances=false for Play in the resolved value. One deliberate continuity carve-out (flagged for founder): `custom_rewards` defaults ON for Play members even with gamification off — the Fun tab always showed the PrizeBox pre-Slice-2, and contract prizes exist independently of gamification; strict Q1 would have silently emptied gamification-off Play kids' prize display.
- `src/hooks/useRewardProvenance.ts` — batch "how they earned it" resolver (tasks / best_intentions / list_items / contract fallback per Section 8 finding #9).
- `src/components/rewards/MyRewards.tsx` — ONE shared sections component (Q3): Points (per-child `gamification_configs.currency_name`, never hardcoded; streak chip), Custom Rewards (PrizeBox + click-in history), Victories (tappable record), Finances (ONE owed number from `useRunningBalance` → `calculate_running_balance` — the SAME query the Balance page uses, so #271 reconciliation holds by construction; tap-through to `LedgerView mode='self'`; never rendered for play).
- `src/components/rewards/RedeemedHistoryModal.tsx` — Previously Redeemed click-in history (Pillar 4/Q8): prize, image, "Earned by: …" provenance, earned/used dates, marked-by attribution.
- `src/components/victories/CelebrationDetailModal.tsx` — narrative via `victory_celebrations.victory_ids` array membership (`celebration_text` dead column never read); celebration-only fallback.

**Modified:**
- `GamificationSettingsModal` — new "My Rewards Page" collapsible section (NOT gated on gamificationEnabled): page toggle (hidden for Play — Fun tab needs no gate), section toggles for the four LIVE sections only (honest controls — creatures/coloring/propose toggles ship with their slices), finances toggle with mirror-default note, `personal_rewards_privacy` toggle for adult members. Local ToggleRow now passes `label` → switch aria-label (a11y + testability). Header comment: 6 → 7 sections.
- `MyRewardsPage.tsx` — stub replaced; effective-member rendering (`useEffectiveMember`), friendly not-enabled card on direct URL, FeatureGuide added. Self-redeem offered ONLY on the member's own real auth session (redeem_own_prize is earner-auth-only).
- `PlayRewards.tsx` — standalone PrizeBox replaced by `<MyRewards variant="play">`; sticker book block kept (becomes the Slice 3 creatures section) and now honors the creatures section opt-in; effective-member aware.
- `GuidedShell.tsx` — **wiring gap found & closed:** Guided shell never renders Sidebar, so Worker 4's sidebar-only entry left Guided kids with NO path to /my-rewards. Added a show_my_rewards-gated "My Rewards" item to the Guided More menu (effective-member aware for View As).
- `PrizeBox.tsx` — additive `hideRedeemed` prop (My Rewards uses the history modal instead of the inline redeemed grid; existing call sites unchanged).

**Proof:** `tests/e2e/features/kids-rewards-slice2.spec.ts` 8/8 — incl. the REQUIRED #271 test (page owed number === `calculate_running_balance` === independent fixture ground truth, with a correct `balance_after` chain), settings writes, Play-shape honesty, finances mirror default, Play finances hard-rule probe, self-redeem + provenance history, victories narrative/fallback, View As (no self-redeem under mom session). Regression: slice1 pin 15/15, leak-pass 10/10, permissions-wiring 14/14, task-assignment-scoping 8/8, fo-command-center 12/12. Tour extended with Stops 6–8 (settings section, Casey full page, View As Jordan) — 8/8 headless dry-run; KRSLICE2-prefixed demo fixtures left in place for live poking, swept by the slice2 suite's next run. `tsc -b` clean, lint 0 errors.

**Founder-attention items:** (a) Play custom_rewards default carve-out above; (b) GuidedShell More-menu entry is in the purpose-built guided nav (exempt from the sidebar single-source rule, parity note recorded); (c) mom direct-URL to /my-rewards renders her own (mostly empty) page under the default-ON ruling; her rewards home remains the Slice 5 PrizeBoard pill per R4-REVISED.

**P0 production save shipped alongside (2026-06-12, FO-COMMAND-CENTER/100265 precedent):** founder console report → reproduced `42P17 infinite recursion detected in policy for relation "meetings"` for EVERY family/user — every meetings read (dashboard recent-meetings + the whole Meetings page) has been silently 500ing in production. Root cause: the cross-table policy cycle `meetings_participant_read` (subqueries meeting_participants) ↔ `mp_mom_read` (subqueries meetings) — migration 100238 had only removed the SELF-recursion, leaving this pair. Fix: **migration `00000000100273`** (applied to linked production, verified OK for founder + Testworth families) — `util.my_meeting_ids()` SECURITY DEFINER helper now backs `meetings_participant_read`, breaking the cycle per the 100265 pattern. Permanent pin: `tests/e2e/features/meetings-rls-recursion.spec.ts` (3/3 — both directions for all roles + participant-visibility behavior preserved). **Next free migration: 00000000100274.**

**Founder eyes-on fixes (2026-06-12, Play shell):**
1. **Play /rewards layout bug (pre-existing, surfaced by eyes-on):** the page rendered `StickerBookDetailModal` INLINE — a full-screen overlay that covered the Play bottom nav AND buried the new Slice 2 sections (Playwright could query them; eyes couldn't see them — exactly why the Visual Verification Standard exists). Now renders the tappable `PlayStickerBookWidget` (dashboard pattern); the full modal opens on tap and closes back to the page. Bottom nav + all sections visible.
2. **Play nav emoji removed → ALL-LUCIDE (round 2 final):** 🏠✅🎮 first replaced with `platform_assets` images, but founder eyes-on found the asset images read inconsistent in the nav ("all lucide icons for now so it looks consistent"). Final state: Home=Home, Tasks=CheckSquare, Fun=Gift — pure Lucide, no `PlatformAssetImage` in the nav. Also swapped the kid-facing 🎉 fallback in `RewardRevealModal` to Lucide PartyPopper.
3. **Play nav was NOT View-As-aware (pre-existing bug, founder "every tab is the same page"):** PlayShell used raw `NavLink`s, so inside View As taps changed mom's REAL router under the modal while the modal kept rendering the dashboard. Plus the modal never mapped `/rewards` and never allowed Play's nav paths. Fixed: PlayShell forks on `isViewingAs` → `useViewAsNav()` (GuidedBottomNav pattern); `renderPage` maps `/rewards`→`PlayRewards`; `allowedPaths` adds Play's paths when `targetShell==='play'`. Pinned by slice2 test 9.
4. **Play nav simplified to TWO tabs — Home + Fun (founder):** two retirements — (a) Stars/`/victories` tab → Fun page Victories section (`victories` defaults ON for Play in `useMyRewardsSettings`); (b) Tasks/`/tasks` tab → Play kids complete on the dashboard via `PlayTaskTileGrid` (Convention #217), the adult Tasks page (13 views/Eisenhower/Kanban) is wrong for Play. `navItems` = Home + Fun; `allowedPaths` play block = `/rewards` only (+ `/dashboard`/`/settings` base). Guided/Independent/Adult navs untouched.
5. **Remaining emoji follow-up (NOT absorbed, per G2 discipline):** ViewAsMemberPicker 👁 (→ Lucide Eye), OneFiveThreeView 📋 empty state, countdown 🎯 fallback + the countdowns `emoji` column feature (`InfoCountdown`/`WidgetConfiguration`) — user-supplied emoji needs a product decision (asset picker instead?). Register at close-out.

**Founder amendments applied same-session (2026-06-12, recorded in Gate-Decisions §11):**
1. **`show_my_rewards` defaults ON** — only an explicit false hides the page/nav entry (founder: "Can we default to on — I don't see it on their pages when I'm doing View As"). Flipped in `useShowMyRewards` + `useMyRewardsSettings`; settings toggle copy updated; slice2 tests 1+2 re-pinned (default-on probe + explicit-off backstop).
2. **Play money = mom opt-in, default OFF** (founder picked the recommended option) — amends Q9/PRD-28's "never on Play" for this surface only. "Money owed" toggle now offered for Play members (default off); explicit opt-in renders the money section on the Fun page (play-sized). Hard-force removed from the resolver; slice2 test 5 re-pinned both directions (absent by default / renders on opt-in). PRD-28 stands on all other surfaces.

## Mom-UI Verification

| Surface | Desktop ≥1024px | Tablet ~768px | Mobile ≤640px | Shells Tested | Evidence | Timestamp |
|---------|-----------------|---------------|---------------|---------------|----------|-----------|
| GamificationSettingsModal — My Rewards section | | | | | Tour Stop 6 (`eyes-on-tour/11`, `12`) — awaiting founder eyes-on | |
| /my-rewards (Guided via View As) | | | | | Tour Stop 8 (`eyes-on-tour/17`, `18`) — awaiting founder eyes-on | |
| /my-rewards (Independent via View As) | | | | | Tour Stop 7 as Casey own-session (`eyes-on-tour/13`–`16`) — awaiting founder eyes-on | |
| /rewards (Play via View As) — incl. small-finger page flip | | | | | Slice 2 sections: Tour Stop 5 (page flip = Slice 3) — awaiting founder eyes-on | |
| Mom's own /my-rewards + self-propose | | | | | | |
| Creature cross-page drag + catalogue | | | | | | |
| Coloring print + download | | | | | | |
| Custom rewards redeem + history + un-redeem | | | | | | |
| Propose-a-Reward (kid form + Queue cards + prefill-confirm) | | | | | | |
| PrizeBoard Prizes arrangement + summary strip | | | | | | |
| Sticker-page + coloring widgets (S/M/L, per shell) | | | | | | |
| RewardConfig / ContractForm / tracker image upload | | | | | | |
| Mobile More-menu parity (`/my-rewards`, incl. mom) | | | | | | |

## Post-Build Verification

*(Checkpoint 5 — every gate requirement: Wired / Stubbed / Missing)*
